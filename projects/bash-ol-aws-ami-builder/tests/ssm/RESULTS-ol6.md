# SSM Agent install+run matrix -- OL6

Generated by `tests/ssm/run-ssm-installtest-matrix.sh` from `ssm-installtest-ledger.json` -- DO NOT hand-edit (regenerated each run).

AWS requirement: SSM Agent **>= 3.3.3598.0** (from 2026-06-16 Run Command drops the legacy `ec2messages` endpoint; agents at/above this use `ssmmessages`).

**Fidelity.** `status=ok` means the RPM installed AND the agent binary ran locally on this `(kernel, glibc)`. The **glibc** axis is faithful (the container's real OL glibc gates a dynamic version). The **kernel** axis is NOT tested in a container -- `kver` is the runner's kernel, so the recorded run reflects it, not the OL's UEK; the per-version **go_version / min_kernel(proxy)** columns are the static kernel-axis signal (the Go toolchain's minimum kernel from the release's go.mod). A faithful kernel verdict needs a kernel-matched runner or a real instance.

## Latest kernel 6.18.5  (glibc 2.12)

Verdict: **compliant-capable** -- max install+run `3.3.4624.0` >= `3.3.3598.0` (remediation possible).

| SSM version | status | ran | glibc | go_version | min_kernel(proxy) | note |
|---|---|---|---|---|---|---|
| 3.3.4624.0 | ok | yes | 2.12 | 1.25 | 3.2 | installed 3.3.4624.0 |
| 3.3.3598.0 | ok | yes | 2.12 | 1.24 | 3.2 | installed 3.3.3598.0 |
| 3.0.1479.0 | ok | yes | 2.12 | 1.15 | 2.6.23 | installed 3.0.1479.0 |
