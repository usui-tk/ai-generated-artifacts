// psa.py latest mainline — project configuration for download-speakerdeck-oracle4engineer
//
// Why PSAP0003 / PSAP0004 / PSAP0005 are enabled:
//   This project's revision history is centralised in CHANGELOG.md per
//   the repository-wide policy documented in the root README.md
//   "Revision History Policy" section. PSAP0003 (inline revision-tag
//   comments) and PSAP0004 (in-script REVISION HISTORY / CHANGELOG
//   comment blocks) enforce that policy: any future commit that
//   accumulates per-revision tags or history blocks inside the
//   PowerShell sources will be flagged by the static-analysis gate.
//
//   PSAP0005 (revision reference in comment body — added in psa.py
//   4.0.0) is the broader LLM-assisted-maintenance guardrail
//   companion of PSAP0003. Where PSAP0003 catches only the structured
//   tag forms (# rNN:, # (rNN), etc.), PSAP0005 catches ANY rNN
//   reference inside a comment body, including descriptive prose
//   anchors. This project opts in to PSAP0005 in STRICT mode (i.e.,
//   `psap0005_relaxed_mode` is NOT set, default false) because:
//
//     - The r21 cleanup commit ("Strip all per-revision inline
//       comments; centralise per-release history in CHANGELOG.md")
//       already removed every rNN reference from the script body.
//     - Verified at r27 / psa-py-v4-llm-governance-baseline:
//       `psa.py 4.0.1 --include PSAP0005` reports 0 findings.
//     - Strict mode is the documented end-state for the
//       upstream sister repo (`usui-tk/Deploy-Drivers-For-WindowsServer`,
//       see its SPEC.md §A.13 "Migration roadmap"); this project is
//       already at the end-state, so the migration-aid relaxed mode
//       is not needed here.
//
// Why PSA6003 is disabled:
//   This script and its companion intentionally use plural-noun function names
//   for functions that operate on collections of resources rather than a single
//   item. Examples in this project:
//
//     - Resolve-RuntimeDirectories  : returns a set of runtime directories
//     - Invoke-CleanupDirectories   : cleans up multiple directory trees
//     - Read-YearOverrides          : reads a table of (filename, year)
//                                     override entries
//
//   Renaming these to singular forms (e.g., -Directory) would either
//   misrepresent the function's behaviour or require breaking changes to
//   callers. The plural form is the more accurate description here, so we
//   suppress PSA6003 at the project level and document the convention in
//   SPEC.md (testing baseline).
//
//   New code added to this directory should still prefer singular nouns where
//   appropriate. PSA6003 is suppressed only because the pre-existing plural names
//   are intentional, not because the rule is unwanted in general.
{
  "enable": [
    "PSAP0003",
    "PSAP0004",
    "PSAP0005"
  ],
  "disable": [
    "PSA6003"
  ]
}
