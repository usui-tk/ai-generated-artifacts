# STATUS — Execution Truth & Handoff

> **Tier-M (in-repo PM layer, M7).** Single living current-truth view + session handoff.
> Bounded (R1): current state only — git history is the audit trail. Scrubbed (R2): no
> secrets/credentials/client data. References, does not restate (R3): the design substrate
> lives in the Tier-P handoff docs (baseline / plan / spine / register) and in `../adr/`.
> Decisions are durable in `../adr/`; this file tracks *status*, not decisions.
> **Session entry point** — read first per the AGENTS.md startup contract. Handoff rule:
> **ADR 0005**. Tier-P design docs stay **unmanaged** (out of repo) and ship as one bundle
> + MANIFEST at each static-point (never piecemeal).

_Last updated: 2026-07-03 (UTC) — **D19 WAVE 2a DONE — 28 units / 110 dd occurrences under watch; only the 4 genuine-code-diff units remain (wave 2b).** Grounding correction that shaped the wave: the comparison report's 'near 12' used a LIGHT normalization; under the drift contract's ACTUAL normalization (**ADR 0015**: strip comments + string literals, collapse whitespace) **8 of the 12 are already equivalent** — framed as-is in the dd wave-2a release (dd `5fc1dec`-authored, tree `6033de5e...`; r89/r55/r33/r37; zero body-byte changes; dd SPEC §A.11.8 gained the normalization-wording rule). This patch registers the **30 wave-2a consumers[] entries** (6 units x 4 sisters + 2 x 3, NPU lacks the 7-Zip pair). Verified: hot 97/7/0 + **skipped 110**; `--satellite` vs the wave-2a dd tree = **207 match / 0 drift**. **Carried: D32 + the first satellite-inclusive cold-loop run (~214 observations).** **Next: wave 2b** — per-unit arcs for `get-sevenzippath` / `enable-debugtracefileoutput` / `export-debugtracejson` / `show-powershellenvironment` — or the post-program menu, user's pick._

---

## Current phase

| Field | Value |
|:---|:---|
| Just completed | **D19 wave 2a (this patch): the 30 normalization-equivalent dd consumer rows registered — 110 dd occurrences under watch.** *(Ledger: Phase progress below.)* |
| Current phase | **D19 — satellite canon distribution (ADR 0030): steps 1-3 + wave 2a DONE; 28 units / 110 dd occurrences watched; wave 2b (4 genuine-code-diff units) remains.** Manifest **90 rows** (58 `powershell-helper` [28 carrying dd consumers] + 7 `tool` [trigger @ 1.1.0, scanner @ 1.1.0, canon-manifest-tool @ 1.1.0, reconciliation-loop @ 1.0.0] + 2 `spec-region` [PS @ 1.0.0, bash @ 1.0.1, tested=true] + 19 `template` + 4 `project` [governed]); 67 released / 19 @ 0.x; Axis-2 1.2.0; ADRs through **0030**. **Carried: D32 + the first satellite-inclusive cold-loop run check.** **Next: wave 2b or the post-program menu — user's pick.** |
| Operating loop | **now formalized in-repo: ADR 0029 + AGENTS §11** (this row remains the one-line reminder): per arc: grounding -> design adjudicated -> `[AUTH]` -> implement -> full battery -> `format-patch` -> fresh-clone `git am` + tree-hash equality -> present -> user pushes -> re-clone re-verify (**Claude never pushes**) |

## Phase progress

| Phase | Status | Notes |
|:--|:--|:--|
| **P0a — Foundation** | ✅ done | scaffold (`governance/ quality-tools/ reference-code/ projects/`), gates, `documents/` consolidation (#19), substrate ADRs, this tracker |
| **psaMove.1** (expand: copy) | ✅ done | byte-identical copy → `quality-tools/powershell-static-analyzer/`; old path coexists |
| **psaMove.2** (expand: migrate/rename) `[AUTH]` | ✅ done | 26 central refs → new path; path-encoding CI workflow renamed; `.ps1` BOM+CRLF preserved; only the 5 old-dir self-refs remain (deleted at psaMove.5, post-P7) |
| **P1.1** — A-4 `@import` gate | ✅ verified | thin `CLAUDE.md` can `@import AGENTS.md` (official Claude Code memory docs); supported as assumed, no deviation; caveats: import is inline (no token saving) + advisory (P1 ships no contract-CI — ADR 0006; wiring stays advisory). See plan P1.1 [VERIFIED] |
| **P1.2–P1.6** — governance model | ✅ done | P1.2 AGENTS.md startup contract (§3) `[AUTH]` · P1.3 thin `CLAUDE.md` `@import` · P1.4 prompt templates (`governance/templates/`) · P1.5 **ADR 0006** (single AGENTS.md source; **no contract-CI at P1**, M2 amends P1 exit) · P1.6 **`governance/SPEC.md`** authored + ADR 0001–0004↔SPEC back-refs closed |
| **P2.1–P2.3** — analyze + classify | ✅ done | inventory of shared functions across 6 consumers; reconcile (consistent + 9 drift, all dispositioned); **39 canonical units** classified (`canonical`/region/follow-latest) + field-ownership map + marker final form |
| **P2.4–P2.5** — schemas | ✅ done | `governance/schema/` `manifest.schema.json` + `observation.schema.json` (draft-07) + `field-ownership.md` |
| **ADR 0007 / P2a insert** — canon functional QA | ✅ done | mandatory full-set canon tests, canon-vs-copy fault attribution, regression-gated reconcile-back; **phase P2a inserted** to author the suite (governs `SPEC §machinery`) |
| **P2.6 (+amend)** — canon code home | ✅ done | `reference-code/powershell/` 39 Public + 19 Private + `.psm1`/`.psd1`; per-unit markers + norm hash; analyzer config (`.psa.config.json` [PSAP0003/4/5] + `PSScriptAnalyzerSettings.psd1`); marker version = **SemVer** (rNN→0.1.0); full config-aware gate 0/0/0 |
| **ADR 0008** — canon release model | ✅ done | SemVer pre-release guardrail: `0.x` not vendorable; promote to `1.0.0` on full-suite pass; vendoring gate `version >= 1.0.0`; refines ADR 0007 |
| **P2.7** — manifest | ✅ done | `governance/state/manifest.jsonl` 58 rows (`canonical_version 0.1.0`, `tested=false`, `consumers=[]`); `tested` added to manifest schema (`maturity` **deferred to TF** — baseline §2.10 CNCF axis, values open until final stage) |
| **P2.8a** — governance-state gate | ✅ done | `quality-tools/governance-state-validator/` (checks A–F: schema · location · manifest↔marker · canon coverage · canonical-JSON); 0 findings on repo, self-test 6/6 |
| **P2.8b** — STATUS + gate clause | ✅ done | STATUS brought current (`10f513d`); AGENTS.md post-flight §14 = governance-state gate |
| **P2 close** — defer manifest maturity | ✅ done | `867efe3`: `maturity` removed from manifest schema/jsonl (deferred to TF — baseline §2.10 CNCF axis); release-readiness via `tested` + SemVer `version` (ADR 0008). P2 closed here |
| **ADR 0009** — psa.py canonical lifecycle | ✅ done | `964752c` `[AUTH]`: psa.py 4.3.0 (`psa2013_known_script_vars`); the `scripts/python/...` psa.py copy frozen at 4.2.0; canon back to 0/0/0 |
| **P2a-prep / Pgov-charter** — ADR 0010/0011 | ✅ done | `56c3dae` **ADR 0010** (canon test taxonomy + JSONL/DuckDB data management); `7027b16` **ADR 0011** (canon change-management governance: manifest = Git-resident master, tool-mediated CRUD [P3a], one process, impact-weighted decision gate, audit trail) + SPEC Machinery back-refs |
| **P2a.1** — canon test home | ✅ done | `98119ed`: `reference-code/powershell/tests/` harness + `CanonSessionState` fixture + Smoke tests + `common/`; governance-state validator check E corrected disk-master → **manifest-master** (ADR 0011 §1; tests/ never flagged), self-test 7/7 |
| **P2a.2** — behavioral suite (58 units) | ✅ done | `879a0da` U0/U1/U2 (41 Pester) + `f38057a` F-state/F-env (Canon.Env). **ADR 0012** (`a3add27` dual-runtime env-info: CLRVersion → `[System.Environment]::Version`, BuildVersion `.ContainsKey` guard) + **ADR 0013** (`3e96cf3` multi-platform/version QA: 3-cell PSScriptAnalyzer compat matrix + per-unit `platform_scope` + classification-backed suppression + FrameworkDescription removal). Real-host verified on PS 5.1 (`4.0.30319.42000`) + PS 7 Linux (`8.0.10`) |
| **P2a.3a** — canon-test gate runner | ✅ done | `ae73339`: `tests/Invoke-CanonTests.ps1` single entry (Pester Unit/Env/Smoke + T11) → **71 passed / 1 skipped / 0 failed, PASS**; canon-resident (vendored with canon); `psa2010_known_cmdlets` for Pester cmdlets |
| **P2a.3b** — SemVer promotion 1.0.0 | ✅ done | `30b67f1`: all 58 units `canonical_version 0.1.0 → 1.0.0` + `tested true`; markers `version=1.0.0` (hash byte-identical); `.psd1` ModuleVersion 1.0.0. **Canon released, vendorable** (ADR 0008 §4: version ≥ 1.0.0) |
| **P2a.4** — close P2a (STATUS) | ✅ done | STATUS brought current to canon 1.0.0 / P2a complete; next phase P3 |
| **ADR 0014** — document governance model | ✅ done | `8249f2e` `[AUTH]` (doc-only): the DOCUMENT analog of ADR 0007+0008. Three cross-repo doc classes (A reference / B own-and-reconstruct / C vendored-copy); `governance/templates/` = document-template canon at code parity (`kind=template` + SemVer release gate + conformance gate over rendered doc-sets); class-(B) reconstruction = graduation structural transform; every reconstruction/sync = a quality gate. **No new phase / no renumbering** (TF + P4–P7 exits absorb it). governs `SPEC §machinery` (bidirectional back-ref). Instance-level wiring deferred to TF.1 |
| **P3.0 / ADR 0015** — canonical normalized-hash contract (P3 opening) | ✅ done | promoted baseline §4.5 → in-repo SPEC §machinery as a **computable** contract (region body → strip comments/strings → collapse whitespace → `sha256`/**16-hex**; gate=normalized, raw=forensic, forked=frozen, whole-tool=null/`n/a`). Validator **check G** (recompute marker hash) + **check D** extended (marker `policy`/`binding` vs manifest); **`quality-tools/canon-hash-restamp/`** write-side tool (metadata-only); conformance pinned by **golden vectors GV-1..5** (no shared import; ADR 0003 reuse-by-copy). Check G caught **20/58 markers mis-stamped at `5d5f0b1`** → re-stamped **no version bump** (bodies byte-identical). Validator self-test **15/15**. Interim **metadata guardrail**: validator (incl. G) green at §Y before patch; bridges to ADR 0011 CRUD tool (P3a) |
| **P3.1 / ADR 0016** — scanner output-contract pins | ✅ done | documents-only structural review (no scanner code yet) pinned 3 undetermined observation-schema fields: **F1** `runtime.duckdb="n/a"` pre-P8 (ADR 0002; runtime stamped not pinned); **F3** `kind`→`granularity` table in SPEC §machinery (manifest has no `granularity`; powershell-helper/bash-region/spec-region→region, python-helper/python-tool/tool→whole-tool, governance-doc out of body-hash-drift scope per ADR 0014); **F4** `drift=unknown` = determinability fallback (conditions pinned at P6). Description-only schema annotations; no structure/code/canon change. Clarified (no decision): **P3 builds+fixture-tests; first real run P6** |
| **P3.6** — scanner build + self-test + register #9 | ✅ done | `quality-tools/canonical-drift-scanner/scanner.py` + `test_scanner.py` (**12/12**): single-file, stdlib-only, no-cross-reference; reuse-by-copies the ADR 0015 normalizer (GV-1..5), marker parse + region extract, normalized+raw hashes, canonical-JSON emit, `granularity` from `kind` (ADR 0016 F3), `runtime.duckdb="n/a"` (F1), `drift=unknown` fallback (F4). Registered as whole-tool unit **#9** (`tool.canonical-drift-scanner`) via the **sentinel-value convention** (`tested`=self-test green, re-defined for whole-tool; cross-platform/canonical/follow-latest/0.1.0) — AGENTS.md §8, not an ADR (**P4.4 decision owed**). Real run pre-P6: whole-tool rows only (ADR 0016 F2) |
| **P3.7** — phase-end (gates + register front-load + static-point) | ✅ done | **P3 phase closed.** Gate battery green (scanner self-test 12/12 + emitted JSONL passes #3; validator A–G + 15/15; restamp in-sync; psa self-check; PSSA 0/0/0; canon-test 71/1/0). `TEMPLATE-REQUIREMENTS-REGISTER` front-loaded for TF.1 (new **§9 TR-TOOL-1/2/3** machinery-registration, P3.6-discovered; reflected in the Tier-P bundle). Static-point taken. Next: **P3a** |
| **P3a.1** — manifest CRUD tool (build + self-test) | ✅ done | `quality-tools/canon-manifest-tool/tool.py` + `test_tool.py` (**13/13**): single-file, stdlib-only; reuse-by-copies the canonical-JSON `_canonical_line` (ADR 0003); CRUD on `manifest.jsonl` (register region/whole-tool · update tested/consumers/non-coupled · deregister). **Self-validating (ADR 0011 §2):** each op is transactional — apply → run the governance-state validator as a subprocess (no shared import; fail-safe if absent) → **roll back byte-identical on any finding**, so the master is never committed invalid. **Write-path boundary (§Z-signed):** manifest-row write path only; a marker-coupled change (`canonical_version`/`change_policy`/`binding_mode` on a region unit, or deregistering one) is **REFUSED** because the validator's checks **D/E/G** catch the resulting marker drift — boundary enforced by the existing gate, not a special case. The coordinated manifest-row+marker write = the **named-deferred `unit-record coupled write`** (P6/P7). **ADR 0015 §6 guardrail superseded for manifest-row edits** (marker edits stay under it / `canon-hash-restamp` until the coupled write — AGENTS.md §8). Manifest unchanged (self-registration is P3a.3). Dry-run (§Y) green on a throwaway clone: real CRUD round-trip + refuse-path verified + full gate battery re-green |
| **P3a.2** — machine trigger (build + self-test) | ✅ done | `quality-tools/canon-drift-trigger/trigger.py` + `test_trigger.py` (**23/23**): single-file, stdlib-only; `_canonical_line` reuse-by-copy (logic byte-identical). The machine entry point of the change-management process (ADR 0011 §3-machine): reads scanner observation records, and for every `granularity=region` + `drift=drift` row emits a thin structured change-request payload on stdout (sorted, canonical-JSON; one per `(unit_id, consumer, region)`). **Emit-only:** no apply, **no new store** (§5); the decision gate (§4) and the `unit-record coupled write` are deferred to **P7a** / **P6/P7**. Every drift-driven request is `classification=marker-coupled`, `proposed_route` = the deferred path — never an immediately-appliable manifest op (consistent with the P3a.1 boundary; the two are non-contradictory — neither applies a coupled change now). **Provisional contract** (no request schema file; P7a owns it) with `contract_status`/`consumer_status` markers (self-test enforces them = deferral stays detectable). Derived only from drift/identity fields; **DEP-3 fork fields not consumed**; `region_locator` opaque pass-through; `kind`/`impact` null (maturity clause). Manifest unchanged (registration is P3a.3); first **real** run is P6. §Y dry-run green on a throwaway clone (Tier 1 static / Tier 2 fixture 23/23 + live emit / Tier 3 full battery / Tier 4 deferral + boundary) |
| **P3a.3** — register CRUD tool + trigger (whole-tool rows) -> **P3a exit** | ✅ done | Registered `tool.canon-manifest-tool` + `tool.canon-drift-trigger` as homogeneous whole-tool rows (`0.1.0`/`cross-platform`/`canonical`/`follow-latest` sentinels per TR-TOOL-1, `tested=true` per TR-TOOL-2, `consumers=[]`) **via the `canon-manifest-tool` CRUD tool itself** (dogfooding; first production use of the P3a.1 write path — each register transactional: apply -> validator -> roll-back-on-finding, both 0 findings). Manifest **59 -> 61** (58 helper + 3 tool). A tool row is a **manifest-only write**, so the CRUD tool handles it **directly** (not refused) — the in-scope counterpart to the refused marker-coupled write (the P3a.1 boundary's "passes" side). **check E bijection unaffected** (powershell-helper-only; 58<->58). finding C: the two rows are homogeneous and do not move the **[P4.4]** convention re-evaluation earlier. §Y dry-run green on a throwaway clone (register x2 + idempotency-reject + full gate battery: validator 0/A-G + 15/15, psa 0/0/0, scanner 12/12, CRUD 13/13, trigger 23/23, PSSA 0/0/0, canon-test 71/1/0) |
| **TF.1** — declare M5 register complete (cycle-closing gate) | ✅ done | Completeness pass: cross-checked every template/dotfile row against each phase's doc-set #4 (no un-entered requirement). Closed **GAP-1** (kind enum `template` — applied at the TF.2 schema edit), **GAP-2** (`template` out of the scanner's body-hash-drift scope), **GAP-3** (doc-set deltas confirmed on disk). Resolved instance-pins: **maturity** (top-level OPTIONAL; live-schema add + per-unit values deferred to graduation), **version-gate = two-axis (C)** (Axis 1 per-template / Axis 2 `governance/templates/VERSION`; durable in **ADR 0018**), **conformance** (machine C1–C8 + human H1–H3), **reconstruction** (9-step procedure), **marker cosmetic** (frozen frame), **template-canon scope** (GAP-4: class-(B) doc-set only; adr/prompt = scaffolding). M5 register (Tier-P) frozen-complete; no body produced yet. |
| **TF.2** — template-canon version model + schema edit | ✅ done | **ADR 0018** (two-axis version model) + `kind` enum `template` + `governance/templates/VERSION` (0.1.0, held at 0.x). |
| **TF (b)-(g)** — doc-governance arc + 1.0.0 split release | ✅ done | **(b)** L1 `doc-format` (58 items + schema + VERSION); **(c)** L2 PowerShell spec home (14 vendored Part A regions); **(d)** 18 doc-set templates (`repo-`/`powershell-`/`bash-`/`python-`, `kind=template`); **(e)** ADR 0020 + `doc_gate.py` (22/22) + spec-home stamp + SPEC §machinery text + 49-region marker roll-out; **(a)** AGENTS vendor-model reconciliation (§2/§5/§6/§7/§8/§9) + §8 gate integration; rule-of-two principle + bash/python L2 deferral; **(f)/(g)** B language-policy fix + C P4.4-retag + **1.0.0 split-promotion** (61 @ 1.0.0 / 20 @ 0.x) + reconstruction freeze + static point. See the **Open pointers** TF bullets for per-step detail. |
| **Governance cross-ref integrity** (F5 + D-γ; pre-P5 hygiene) | ✅ done | **F5:** the `governs` SPEC-anchor casing was inconsistent — ADR 0009/0010/0011 used `§Machinery` vs the canonical lowercase `§machinery` (the dominant convention + the GitHub anchor of `## Machinery`); fixed the 3 ADRs (full-scan: 0 capital `§Machinery` remain). **D-γ:** implemented the ADR 0014 §4.10 MANDATORY ADR↔SPEC integrity gate by **extending `doc_gate`'s C6 to the live `governance/adr/` + `SPEC.md` corpus** (form (c) — keeps the governance-state validator reference-code-scoped per the ADR 0020 boundary; doc_gate owns doc inspection). v1 = all 4 checks (governs-resolves-by-canonical-anchor, bidirectional back-ref, superseded-not-live, supersedes/superseded_by symmetry; `governs=[]` carve-out for process/meta ADRs). **fix-then-gate** demonstrated: pre-fix the gate flagged exactly the 3 F5 cases; post-fix PASS 0. doc_gate self-test 22/22 → **33/33** (+11 C6). No new ADR (covered by §4.10 / ADR 0014 + ADR 0020); no new tool row (no P4.4 interaction). §Y dry-run green on this clone (full battery below). |
| **P5** — bash `ol-aws` same-repo migration (graduation + reconstruction) | ✅ done | **doc-org prerequisite:** added + CRUD-registered the `template.bash.spec` **structural** template (no vendored Part A; no spec home — rule-of-two); manifest 81 → 82. **doc_gate shakedown (P5-A/B/C):** repointed `check_front_matter` at the ADR 0019 doc-provenance block; added the L3 `--reconstructed` light-conformance mode (provenance / bilingual lock-step / encoding / absolute governance links); reconstructed class-(B) docs carry NO markers (ADR 0019 §7/§88). doc_gate self-test 33 → **39**. **Release (ADR 0018):** Axis-2 `governance/templates/VERSION` + `governance/doc-format/VERSION` 0.1.0 → **1.0.0** (reconstructability gate; clears C5 for P6) + `tool.document-conformance-gate` 0.1.0 → 1.0.0 (proven). Per-template Axis-1 versions stay 0.1.0 (provenance). **Graduation:** `git mv scripts/aws/ol-aws-ami-builder → projects/bash-ol-aws-ami-builder` (first `projects/` graduate); doc-set reconstructed from canon v1.0.0 (doc-provenance pins, self-asserted governance stripped, absolute links, new CHANGELOG); `.sh` + `env.properties` moved unchanged; reference-into rewired (SECURITY / AGENTS / CONTRIBUTING / scripts-README). Verified by `doc_gate --reconstructed` (L3 PASS 0/4) + full battery. Version split **62 @ 1.0.0 / 20 @ 0.x**. **Promotion split:** `spec.powershell.part-a` promoted at P6. |
| **P4** — psa.py promotion to governed machinery (`[P4.4]` resolved) | ✅ done (2026-06-07, step 0 of PS-migration prep) | Registered **`tool.powershell-static-analyzer`** (`kind=tool`, `canonical_location=quality-tools/powershell-static-analyzer`, own SemVer **`4.3.0`**, sentinels `canonical`/`follow-latest`/`cross-platform`, `tested=true` from self-test 281/0) via the **CRUD tool** (dogfood; transactional, validator 0 findings); manifest **82 → 83**, tool rows **4 → 5**. **`[P4.4]` RESOLVED:** rule-of-two met by psa.py as the SemVer-heterogeneous 2nd whole-tool instance → the sentinel-value convention is promoted to **ADR 0021** (TR-TOOL-1/2/3), with a **SPEC §machinery** subsection (current-truth view + back-ref) and the **AGENTS §8** block reconciled (the `[Deferred to P4.4]` marker removed; ADR 0008 vendoring gate noted as not applicable to whole-tool units). No new principle ADR needed (ADR 0003 + 0009 already cover psa.py). **doc-set (D2):** psa.py's copied `README.md`/`README.ja.md` relative links depth-corrected for the `quality-tools/` home (`../../../` → `../../`; `../../powershell/` → `../../scripts/powershell/`); 16 links now resolve (machinery docs are out of the doc_gate conformance corpus, so no gate finding either way). psa.py code + the canon untouched. |
| **P6a** — `download-speakerdeck-oracle4engineer` migration (shakedown consumer) | ✅ done | Commits `0bda579` (canon: `Set-TlsSecurityProtocol` promoted to the Tls1.3-inclusive body — drift decision B — hash restamped, version held 1.0.0) · `7da2bf2` (machinery: `canonical-drift-scanner` `region_locator` finalized for multi-region consumers; self-test 12 -> **16/16**) · `3cd181e` (consumer): `git mv` to `projects/powershell-download-speakerdeck-oracle4engineer`; **39 code-regions** vendored from canon; SPEC Part A reconstructed as **14 vendored doc-regions + A.14** (Debug Trace cross-script feature); doc-set front-matter + absolute links; **R-2 fold-in** (repointed `update-iso`'s Part A reference to the spec home, avoiding a broken intermediate); `consumers[]` registered (40 units); **first real scanner run** (resolved the deferred `region_locator` / `drift=unknown` items). Gate set **no offline-test** (no `tests/` -> TESTING manual-procedure presence). doc_gate path + reconstructed PASS. Verified live (tree-hash). |
| **P6b** — `update-windows-server-iso` migration (complex consumer) -> **P6 complete** | ✅ done | Commit `7566d22` (applied on the intervening governance-neutral ol-aws `f1e44a4`). `git mv` to `projects/powershell-update-windows-server-iso`; **58 inlined canon helpers** vendored = 53 clean-wrap + **5 reconcile-to-canon** (`Set-TlsSecurityProtocol` Tls1.3 · `Export-DebugTraceJson` `procId`/`hostName` JSONL rename · `Initialize-RuntimeDirectories` parameterized + call-site · `Show-PowerShellEnvironment` unify · `Write-PhaseFooter` 4-value `Status` incl `cached`); **`$ks` PSScriptAnalyzer deviation REMEDIATED** (dead assignment removed; KB digits still regex-extracted; PSScriptAnalyzer 0/0/0); SPEC Part A reconstructed (14 vendored doc-regions + A.14); doc-set front-matter + absolute/psa-retarget links; `consumers[]` on 58 helpers + spec-region (59 units). **Full battery incl. offline-test 20/0** (incl. `powershell_harness` driving the vendored script) + Pester 71/0 + doc_gate path/reconstructed. Verified live: the patch on the new base `f1e44a4` reproduces the live tree byte-identically. |
| **B0** — bash family: rule-of-two extraction `[AUTH]` | ✅ done | Trigger: 2nd Bash consumer `projects/bash-rhel-container-testsuite` (r27). Extracted **`governance/spec/bash.md`** (8 Part A regions A.1-A.8, distilled from conventions observed in BOTH consumers; A.5 two-tier shell-option scope corrects the first consumer's blanket text; new substitution-inheritance asymmetry rule, grounded by an empirical `-e` audit). `bash-spec.template.md` inline-Part-A -> vendored model (Axis-1 0.2.0); Axis-2 `templates/VERSION` **1.1.0**. AGENTS.md §5 (navigational-label rule) / §6 / §7 reconciled; `governance/SPEC.md` spec-home path fixed. Manifest 83 -> **84** (`spec.bash.part-a` @ 0.1.0 registered + `template.bash.spec` -> 0.2.0, CRUD tool, validator 0 findings). Next: **B1** (ol-aws consumer migration), **B2** (rhel doc-set reconstruction; code alignment first per approved D1-D4) |
| **B1** — ol-aws consumer migration to vendored bash Part A | ✅ done | SPEC Part A: 8 vendored regions (doc_gate hash-verified vs the home) + extensions A.9-A.17 (all single-observer content preserved; old->new map in project CHANGELOG). Cross-refs remapped in Parts B-E + TESTING; front-matter -> template canon 1.1.0; TESTING.md pin added; CHANGELOG intro ground-truthed. AGENTS par.7 pointers remapped. Manifest consumers[] += ol-aws (validator 0 findings). Docs-only; OL suite green. Next: **B2** |
| **B2.code** — rhel-container-testsuite canon alignment (r28) + A.3 re-scope | ✅ done | Canon: A.3 role-scoping (ground truth: OL auxiliaries use minimal log(); restamp + OL re-sync; D2 closes as already-conformant). r28: -euo on 8 production scripts (harness -uo + rationale); 3 result-line guards (A.5 asymmetry, empirically grounded); Layer-1 five-section headers on 42/42 .sh. Suite 478 x3; ShellCheck default+style clean; RESULTS byte-stable. Next: **B2.docs** |
| **B2.fix** — t002 CWD-independent ShellCheck source-path (r29) | ✅ done | The two "unreproduced flakes" root-caused as deterministic CWD dependence (pre-existing; r27 reproduces): t012/t014 `source=lib/...` -> SC1091 at `-S style` unless CWD=project root. t002 += `-P "${PROJ}"`; 3x green from repo root + 3x from project dir. SPEC D.8 records the lesson |
| **B2.docs** — rhel doc-set reconstruction to the template canon (r30) | ✅ done | SPEC: 8 vendored regions + A.9/A.10 extensions; Part B.1-B.13; Part C (R5-R8 + Q1/Q2, Q3 superseded); Part D.1-D.8. ADDING-A-TOOL folded into bilingual READMEs (deleted); README counts + Known limitations + Provenance (lock-step); TESTING t017-t020 + L0=42 + re-pin; CHANGELOG pinned. Manifest consumers[] += rhel. **B0->B2 arc complete: bash family fully on the spec-home model** |
| **G0** — safety + knowledge fixation (program re-anchored to the G-program) | ✅ done | doc_gate **C9** version coupling (closes the PROVEN 2026-06-11 spec-region hole; FAIL demonstrated) + **ADR 0022/0023** (durable decisions land in-repo) + STATUS currency restored |
| **G1+G2** — lifecycle maturity model + exploration/RE mode | ✅ done | `maturity` axis LIVE (`kind=project` rows, schema allOf, CRUD `--maturity`; 4 projects `governed`; **ADR 0024** incl. the TF.1 `graduated`->`governed` supersession) + exploration mode official (**ADR 0025**: light gates, HARD boundary, conformance-pass exit) + AGENTS §10 bootstrap + scaffold prompt (Axis-2 1.2.0) |
| **G3** — change-resilience | ✅ done | reference-health gate (**ADR 0026**; gate-then-fix 19 -> 0 findings over Layer-0) + `gate-coverage.md` inventory + STATUS R1 pruning + Tier-P⇄Tier-M sync operationalized ([HANDOFF-DOC] resolved) |
| **G4.1** — R-3.1 coupled write + R-3.2 promotions + step-8 close | ✅ done | `promote` op (snapshot transaction, validator + doc_gate incl. C9 subprocess gates, byte-identical rollback; CRUD self-test 26/26) + BOTH spec-regions -> **1.0.0 tested=true** (42 + 24 markers, throwaway-rehearsed) + same-repo governance eval CONFIRMED |
| **P7** — Deploy-Drivers separate-repo migration + first cross-repo satellite | ✅ done | 62 live psa refs -> the governed home (D15: CHANGELOG historical; grounding corrected the stale '8 refs') + **class-(C) AGENTS.md thin bridge + CLAUDE.md `@import`** (Topic C1; master = this repo; absolute-URL references; rule-of-two defers the satellite template) — merged `3bda89b`, tree `25fe0573...`; **zero-LIVE-referrer HOLDS (D16) -> psaMove unblocked**; D19 deferrals: dd manifest rows / vendored regions / fetch-based drift gate |
| **psaMove CONTRACT** — delete the retired `scripts/` tree | ✅ done | §9.3 human checkpoint honored; 33-edit rewiring (grounding widened 2 named spots to the full inventory incl. pre-existing broken links) + `spec.bash.part-a` **1.0.1** via the coupled op + the 10-file delete-only commit + SPEC/STATUS close; lineage via `--follow`; **expand/contract (§2.12) complete** |
| **P7a** — decision gate + AI trigger + machine impact (ADR 0011 §3-AI/§4) | ✅ done | trigger **1.1.0** (`impact` + `propose` + pending-decision drift path; contract pinned @ 1.0.0; heavy-path refusals) + **ADR 0027**; retrospective validation exact (24/42 markers); self-test **33/33**; ADR 0011 principles 1-5 live end-to-end |
| **P8** — the reconciliation cold loop (scheduled scan + DuckDB + ledger) | ✅ done | coldloop **1.0.0** (12/12; skip-key dedup; append-only byte-verified; write surface enforced) + daily workflow (loop double-guard); **ADR 0028**; `[OPEN-exec]` settled (cadence daily / paths fixed / metric = unit x reason-class); hot/cold observation discipline split; **live-schedule check deferred to the first post-push run**; + psa workflow pre-rename path-filter fix (SPEC-CI-070) |
| **two-speed** — the operating model formalized (the FINAL G-program item) | ✅ done | **ADR 0029** (2 streams / 5 rules; options recorded) + **AGENTS §11** thin summary + update-trigger bullet; docs-only; **G-PROGRAM COMPLETE** |
| **D19(1)** — satellite canon distribution: ADR + central machinery | ✅ done | **ADR 0030** (D33-D36: waves / `consumers[].repo` / cold-loop satellite scan / ordering) + manifest schema + scanner **1.1.0** (`--satellite`, per-repo partitioning, skip-not-fail semantics; version constant realigned 0.1.0->1.1.0 with the row) + the cold-loop dd clone step (inert until step 3) + validator/scanner self-tests 19/19 each; routed via `trigger.py propose` — **D23 forward validation** (feature/minor/medium, request contract 1.0.0) |
| **D19(2)** — dd wave-1 framing release (satellite, user PR) | ✅ done | dd release `cross-repo-canon-vendored-region-markers-wave-1` (Chipset r88 / Graphics r54 / NPU r32 / BthPan r36): 20 pure-EXACT units x 4 sisters = **80 marker-framed occurrences, zero body-byte changes** (mechanically proven) + dd SPEC **§A.11.8** (markers = local no-touch zone; composes with PSA8001) — merged dd `2c2ad00`, tree `6bdd0892...`; psa 0/0/0 |
| **D19(3)** — cross-repo consumer registration; the watch goes LIVE | ✅ done | `--add-consumer` OPTIONAL `repo=` key (unknown keys refused — latent silent-drop closed; canon-manifest-tool **1.1.0**, propose-routed, self-test 26 -> **28**) + **80 consumers[] entries** via 20 tool transactions (validator green each) — hot runs report **skipped 80** (satellite-blind by design); `--satellite` vs merged dd = **177 match / 0 drift** |
| **D19(4a)** — wave 2a: the 8 normalization-equivalent units framed + registered | ✅ done | Grounding correction: under the ADR 0015 normalization (comments/strings/whitespace ignored BY DESIGN) 8 of the 'near 12' are already equivalent — dd release wave-2a (r89/r55/r33/r37, tree `6033de5e...`, 30 framed occurrences, zero body-byte changes, dd SPEC §A.11.8 normalization-wording rule) + **30 consumers[] entries** (8 tool transactions, validator green each) — hot **skipped 110**; `--satellite` = **207 match / 0 drift**. Wave-2b remainder: 4 genuine-code-diff units |

Gates green (re-verified each phase; latest = D19 wave 2a / this patch): psa.py **carried** (config + canon byte-unchanged) · PSScriptAnalyzer **0/0/0** + **canon-test runner PASS (71/1-skip/0)** — both **carried by subtree byte-equality** (`reference-code/powershell` tree-hash identical to the last full pwsh verification at `cf06348`; the P4 determinism fact) · governance-state-validator **0 findings (A-G)** (**90 manifest rows** = 58 `powershell-helper` + 7 `tool` + 2 `spec-region` + 19 `template` + 4 `project`; 58 helper rows <-> 58 canon files [check E]; self-test **19/19** incl. the `consumers[].repo` green/red pair) · **`canon-hash-restamp --check` IN SYNC (58/58)** · **`canon-manifest-tool` self-test 28/28** (incl. the `promote` coupled-op set + the `repo=` consumer-spec pair) · **`canon-drift-trigger` self-test 33/33** (incl. the P7a decision-gate set); **`reconciliation-loop` self-test 12/12** (cold path; needs `pip install duckdb`) · **`canonical-drift-scanner` self-test 19/19** (+ hot run 97 match / **7 n/a**, drift 0, **skipped 110** [the dd rows — satellite-blind by design, ADR 0030]; `--satellite` vs the merged dd checkout = **207 match / 0 drift**) · **`doc_gate` self-test 45/45 + default PASS 0/18** (incl. C9 version coupling + C6 ADR↔SPEC integrity) + `--path` per consumer SPEC PASS + `--reconstructed` L3 PASS (ol-aws 0/4; iso 0/5; dsd 0/5; rhel 0/5) · **`reference-health-gate` self-test 10/10 + real run PASS 0 findings / 9 scoped files** (R1/R2/R5, ADR 0026) · **version split: 67 released / 19 @ 0.x** (PS @ **1.0.0**, bash @ **1.0.1**; both tested=true) · **template-canon Axis-2 `templates/VERSION` = 1.2.0**. Runtimes: python3 · jsonschema 4.26.0 (pwsh gates carried per the subtree-equality fact above).

## Next action
**D19 steps 1-3 + wave 2a are done (110 dd occurrences watched). Remaining, then the menu:**

1. **D32 + the first satellite-inclusive cold-loop run (operational, first):** the daily 03:17 JST run should be green AND commit ~214 observations (97+7 central + 110 dd) split across `repo=ai-generated-artifacts` and `repo=Deploy-Drivers-For-WindowsServer` partitions, `[skip ci]`, no retrigger, **0 drift proposals**. Record as a 1-line maintenance note.
2. **D19 wave 2b — per-unit reconciliation arcs** (ADR 0030; one judgment each): `get-sevenzippath` (dd lacks the canon's Windows-neutral non-Windows fallback — canon-adoption candidate), `enable-debugtracefileoutput` (canon's defensive `procId`/`hostName` rename vs dd's `pid`/`host` JSONL keys — canon-adoption candidate with a dd log-schema review), `export-debugtracejson` (dd's refactor may be RICHER — propose-reflow candidate; needs the full diff), `show-powershellenvironment` (Δ200 structural — needs the full diff; adoption vs fork open).
3. **Then the remaining menu** (user's pick): derivations (`--reconstructed` lists + the STATUS stream table from manifest project rows) / TF d2w (`github-workflows` template family) / first sandbox project via AGENTS §10.

**Carried deferrals (all tracked, additive):** D32 + the first satellite-inclusive cold-loop run (above); D19 wave 2b — 4 per-unit arcs (above); fetch-based cross-repo HOT gate — deferred by ADR 0030 (grounded-need); satellite-AGENTS template — rule-of-two; bash/python doc-set L2 remainder — rule-of-two; `reference-code/bash/` — grounded-need; code-marker coupled promotions — restamp suffices (D11).

## Open pointers
- `[RESOLVED at psaMove]` **psaMove CONTRACT executed**: rewiring -> coupled 1.0.1 -> delete-only commit (§9.3 checkpoint honored); the old-dir self-refs left with the tree. Historical mentions (both repos' CHANGELOGs, ADR 0009's decision record, this ledger) are the sanctioned remainder per D16.
- `[RESOLVED at P7]` **Deploy-Drivers old-path references**: all 62 live occurrences migrated (`3bda89b`); the stale '8 references' figure was corrected by grounding; CHANGELOG's 8 historical lines exempt (D15/D16).
- `[RESOLVED at G4.1]` **Spec-region promotions**: both families are at **1.0.0 tested=true** via the R-3.1 `promote` op; the ADR 0008 pre-release exception is closed. Future doc-region promotions use `promote` (a manifest-only write still fails doc_gate C9, ADR 0022).
- _(The completed-TF execution log formerly kept here was pruned at G3(c) per R1 — the durable record is the Phase-progress ledger, git history, and ADRs 0018-0021.)_

## ADR index (`../adr/`)
- 0001 — tooling language = Python · 0002 — analysis layer = DuckDB (disposable, P8) · 0003 — standalone-tool principle · 0004 — outcome-based execution framework (M8) · 0005 — session-handoff protocol · 0006 — AI-agent config coverage & contract-CI scope · **0007 — canon code functional QA** (P2a) · **0008 — canon release model** (SemVer pre-release guardrail; refines 0007) · **0009 — psa.py canonical lifecycle** (single canonical psa.py at `quality-tools/`; `scripts/` copy frozen) · **0010 — canon test taxonomy + data management** (test buckets; JSONL master + DuckDB ephemeral) · **0011 — canon change-management governance** (manifest = Git-resident master, tool-mediated CRUD, one process, impact-weighted decision gate, audit trail) · **0012 — dual-runtime env-info policy** (equivalent-quality info on PS 5.1 + 7.x) · **0013 — multi-platform / multi-version QA** (3-cell compat matrix + per-unit `platform_scope` + classification-backed suppression) · **0014 — document governance model** (cross-repo doc classes A/B/C; `governance/templates/` = document-template canon at code parity with version + conformance gate; class-(B) reconstruction incl. the graduation structural transform; every reconstruction/sync = a quality gate; the DOCUMENT analog of ADR 0007+0008; no new phase, TF + P4–P7 exits absorb it; **scope clarification 2026-06-04: governs documents + masterless repo-structural dotfiles, NOT tool-owned configs (e.g. `.psa.config.json`, master = tool canon, follow-latest per ADR 0009) — discriminator = master-ownership**) · **0015 — canonical normalized-hash contract** (the computable hash definition promoted from baseline §4.5: region body → strip comments/strings → collapse whitespace → `sha256`/16-hex; validator read-side **check G** + marker `policy`/`binding` check; write-side `canon-hash-restamp` tool; golden-vector conformance; interim metadata guardrail bridging to the ADR 0011 CRUD tool; corrected 20 markers mis-stamped at `5d5f0b1`, no version bump). · **0016 — scanner output-contract pins** (P3.1; documents-only structural review: `runtime.duckdb="n/a"` pre-P8 [F1], the `kind`→`granularity` derivation table in SPEC §machinery [F3], and `drift=unknown` as the determinability fallback framed now / detailed at P6 [F4]; description-only schema annotations; no structure/code change). · **0017 — structure-first review methodology** (a→b: verify structural validity in the normative set first, investigate current state/current artifacts second; re-scan + forward/back impact analysis + now-vs-later split on each finding; the epistemic-order complement to ADR 0004; finalized after P3's full cycle). · **0018 — template-canon version model (two-axis)** (refines ADR 0014 §2: the template canon carries TWO version axes — **Axis 1** a per-template body `canonical_version` for provenance; **Axis 2** a single `governance/templates/VERSION` that is the reconstructability gate + the consumer pin target. The gate axis follows the unit of delivery — per-unit for vendored code (ADR 0008), per-set for reconstructed doc-sets (ADR 0014 class-(B)). Axis 2 is a release-commit artifact outside the manifest, like the code canon's `.psd1` ModuleVersion, so the ADR 0011 CRUD manifest-row write-path boundary is unaffected). · **0019 — document vendor model + provenance embedding** (refines ADR 0014; evolves AGENTS.md §6 + TR-SPEC-2: three-layer doc model — **L1** language-independent format / **L2** language template [common = vendored-from-code + subproject-specific] / **L3** rendered doc-set; L2-common is **vendored** into L3 via a managed region (marker+hash+tool-sync), not referenced; provenance embedded two ways — doc-level YAML front-matter pin + region-level ADR 0015 markers — feeding a two-level staleness trigger; variance = explicit applicability metadata, no conditional fold-in; heavy marker/hash machinery localized to L2-common while L1/L3 stay light structural conformance). · **0020 — doc-region normalized-hash contract** (hash = {common-fixed, vendored}; structural for common-parameterized/specific/mixed; the document-conformance gate owns all doc-region inspection [`--check`/`--stamp`]; validator D/G + canon-hash-restamp stay reference-code-scoped, NOT extended to markdown) · **0021 — whole-tool registration convention** (sentinel values for the region-helper required fields on a `kind=tool` row; `tested`=self-test-green re-defined for whole-tool; the ADR 0008 vendoring gate does NOT apply to whole-tool units; rule-of-two met by psa.py as the SemVer-heterogeneous 2nd instance; resolves the `[P4.4]` decision; governs SPEC §machinery). · **0022 — spec-region version coupling (doc_gate C9)** (follow-latest marker `version=` == manifest `canonical_version`; pin may lag, never lead; closes the proven 2026-06-11 hole) · **0023 — durable decisions land in-repo** (as ADRs, regardless of session patch output; Tier-P is a convenience, not the master) · **0024 — project lifecycle maturity model** (`kind=project` rows; sandbox/incubating/governed/archived; the TF.1 `graduated`->`governed` supersession) · **0025 — exploration/RE mode** (light gates, HARD boundary, conformance-pass exit) · **0026 — reference-health gate** (refcheck over Layer-0 root docs: R1 link targets / R2 workflow names / R5 STATUS row-count claims) · **0027 — decision gate + AI trigger** (machine impact measurement; kind->SemVer->tier; heavy-tier refusal without consumers+migration+ADR; `propose`; request contract pinned 1.0.0) · **0028 — reconciliation cold loop** (scheduled observe -> DuckDB aggregate -> append-only proposals ledger; hot/cold split; `[skip ci]` loop safety) · **0029 — two-speed operating model** (one `main`, maintenance vs governance streams, five rules; AGENTS §11 thin summary) · **0030 — satellite canon distribution** (dd: two waves; `consumers[].repo`; cold-loop satellite scan with hot-path skip; ordering; ALL dd changes via user PRs).
- Cross-cutting current-truth view: **`../SPEC.md`** — §tooling/§analysis-layer/§machinery/§execution-framework ← ADR 0001–0004; **§machinery also ← ADR 0007/0008/0011/0012/0013/0014/0018/0019** (bidirectional `governs`↔back-ref).

## Design substrate (Tier-P handoff — referenced, not restated, R3)
Out-of-repo per **ADR 0005** (unmanaged; ships as one bundle at each static-point, never piecemeal). **Current inventory (the startup bundle, restructured 2026-07-03 into 3 layers): `STARTUP-CORE.md`** (shared session mechanics) **+ per-stream `STARTUP-<stream>.md` slots** (current-pointer snapshots; `SESSION-STARTUP.md` remains the self-contained entry file) **+ `DESIGN-SUBSTRATE.md`** (low-churn: consolidated design baseline / implementation plan / traceability spine / template-requirements register as Parts I-IV, plus dated session-update blocks — newest block supersedes older text). *Resolution note ([HANDOFF-DOC], 2026-07-03): ADR 0005 §2 names the original four design documents; that list is the historical initial inventory — the four documents were consolidated into `DESIGN-SUBSTRATE.md` Parts I-IV, and THIS section is the living inventory (R3: reference, don't restate). The ADR's decision (Tier-P unmanaged / STATUS entry / whole-bundle static-points) is unchanged; per ADR 0023, durable decisions always land in-repo as ADRs regardless of session patch output.*

## Static-point index (recoverable points = commits; repo-external Zips per `../static-point-procedure.md`)
| Phase | Commit | Static-point |
|:--|:--|:--|
| P0a | `5490de3` (+ `de39ef9` cleanup) | per procedure (repo-external Zip) |
| psaMove.1 | `f289933` | per procedure |
| psaMove.2 | `f8b942b` | per procedure |
| P1 (governance model) | `1097309`·`dd8f9f3`·`cab8375`·`9a05635`·`9be1056` | per procedure (repo-external Zip at P1 phase-end) |
| P2 (analyze + canon + manifest + gate) | `597638e` … `867efe3` | _repo-external Zip at P2 phase-end_ |
| P2a (canon test suite + release 1.0.0) | `964752c` … `30b67f1` | _repo-external Zip at P2a phase-end (this phase)_ |
| P3 (normalized-hash contract + drift scanner) | `30b67f1` … `90117ca` | per procedure (ADR 0015/0016 + scanner; P3 phase-end) |
| P3a (canon change-mgmt machinery: CRUD tool + trigger + registration) | `90117ca` … `648033e` (registration) · `59c100c` (P3a STATUS exit) | _repo-external Zip at P3a phase-end (this phase)_ |
| TF (doc governance: L1/L2 + gate + AGENTS reconciliation + 1.0.0 split release) | … `90117ca` → **1.0.0 split release** | `ai-generated-artifacts-TF-20260605T194306Z-static-point.zip` (repo-external, this phase-end) |
| P6 (PowerShell consumer migration: P6a download-speakerdeck + P6b update-iso) | `0bda579`·`7da2bf2`·`3cd181e` (P6a) ... `7566d22` (P6b) | per procedure (repo-external Zip at P6 phase-end) |
