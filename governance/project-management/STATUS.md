# STATUS — Execution Truth & Handoff

> **Tier-M (in-repo PM layer, M7).** Single living current-truth view + session handoff.
> Bounded (R1): current state only — git history is the audit trail. Scrubbed (R2): no
> secrets/credentials/client data. References, does not restate (R3): the design substrate
> lives in the Tier-P handoff docs (baseline / plan / spine / register) and in `../adr/`.
> Decisions are durable in `../adr/`; this file tracks *status*, not decisions.
> **Session entry point** — read first per the AGENTS.md startup contract. Handoff rule:
> **ADR 0005**. Tier-P design docs stay **unmanaged** (out of repo) and ship as one bundle
> + MANIFEST at each static-point (never piecemeal).

_Last updated: 2026-06-04 (UTC) — **TF.2: ADR 0019 (document vendor model + provenance embedding) authored — doc governance re-framed into THREE LAYERS (L1 language-independent format / L2 language template [common = vendored-from-code + subproject-specific] / L3 rendered doc-set); L2-common is VENDORED into L3 via managed regions (marker+hash+tool-sync), NOT referenced; provenance = a doc-level YAML front-matter pin + region-level ADR 0015 markers, for two-level mechanical staleness triggering; variance = explicit applicability metadata, no conditional fold-in.** Evolves AGENTS.md §6 (Part A Inheritance) + TR-SPEC-2 (a separate [AUTH] §6 edit follows, recorded against ADR 0019). **Program = 4 buckets:** (1) governance baseline [P0a–TF, incl. the deferred P2.6 spec home `governance/spec/powershell.md`] → (2) same-repo migration [P5 bash, P6 PS] → (3) separate-repo migration [P7 Deploy-Drivers] → (4) cleanup [psa.py `scripts/` copy; P7a/P8]; **baseline 1.0.0 freeze is the single gate before migration.** Prior: ADR 0018 / schema `kind` enum + `VERSION` / TF.1 register-complete._

---

## Current phase

| Field | Value |
|:---|:---|
| Just completed | **TF.2 — authored ADR 0019 (document vendor model + provenance embedding)** + SPEC §machinery back-ref + STATUS. Re-frames doc governance into 3 layers (L1/L2/L3); L2-common **vendored** into L3 via managed regions (not referenced); provenance = doc-level YAML front-matter pin + region-level markers (2-level staleness trigger); variance = applicability metadata (no fold-in). Evolves AGENTS.md §6 + TR-SPEC-2 (separate [AUTH] §6 edit to follow). *(Prior: **ADR 0018**; schema `kind` enum + `VERSION`; **TF.1** register-complete.)* |
| Current phase | **TF.2 — baseline document model, in progress** (ADR 0014 + 0018 + 0019). Doc governance: **L1** language-independent format / **L2** language template (common = vendored-from-code + specific) / **L3** rendered doc-set; L2-common **vendored** into L3 (managed region, marker+hash+tool-sync); provenance front-matter pin + region markers. **Remaining baseline (dependency order):** §6 evolution edit ([AUTH], vs ADR 0019) → **L1 format** (formalize the M5 register) → **L2-PS common** `governance/spec/powershell.md` (**deferred P2.6** spec home; vendored, marker+hash, `spec-region` rows) → **L2 templates** (PS + bash, family-parameterized) + `kind=template` registration → **document-conformance gate** (structural + region-hash) → **reconstruction procedure** → **TF freeze** (template canon 1.0.0 + static-point). **Then migration:** P5 bash / P6 PS (same-repo) → P7 Deploy-Drivers (separate-repo) → cleanup (psa.py `scripts/` copy). **Single gate:** baseline 1.0.0 freeze before any migration. **Off critical path:** psa.py governance (P4, python-tool family). |
| Operating loop | per phase: fill per-step schema → dry-run §Y → sign-off §Z → execute (Path 2) |

## Phase progress

| Phase | Status | Notes |
|:--|:--|:--|
| **P0a — Foundation** | ✅ done | scaffold (`governance/ quality-tools/ reference-code/ projects/`), gates, `documents/` consolidation (#19), substrate ADRs, this tracker |
| **psaMove.1** (expand: copy) | ✅ done | byte-identical copy → `quality-tools/powershell-static-analyzer/`; old path coexists |
| **psaMove.2** (expand: migrate/rename) `[AUTH]` | ✅ done | 26 central refs → new path; path-encoding CI workflow renamed; `.ps1` BOM+CRLF preserved; only the 5 old-dir self-refs remain (deleted at psaMove.5, post-P7) |
| **P1.1** — A-4 `@import` gate | ✅ verified | thin `CLAUDE.md` can `@import AGENTS.md` (official Claude Code memory docs); supported as assumed, no deviation; caveats: import is inline (no token saving) + advisory (P1 ships no contract-CI — ADR 0006; wiring stays advisory). See plan P1.1 [VERIFIED] |
| **P1.2–P1.6** — governance model | ✅ done | P1.2 AGENTS.md startup contract (§3) `[AUTH]` · P1.3 thin `CLAUDE.md` `@import` · P1.4 prompt templates (`governance/templates/`) · P1.5 **ADR 0006** (single AGENTS.md source; **no contract-CI at P1**, M2 amends P1 exit) · P1.6 **`governance/SPEC.md`** authored + ADR 0001–0004↔SPEC back-refs closed |
| **P2.1–P2.3** — analyze + classify | ✅ done | inventory of shared functions across 6 consumers; reconcile (consistent + 9 drift, all dispositioned); **39 canonical units** classified (`canonical`/region/follow-latest) + field-ownership map + marker final form |
| **P2.4–P2.5** — schemas | ✅ done | `governance/schema/` `manifest.schema.json` + `observation.schema.json` (draft-07) + `field-ownership.md` |
| **ADR 0007 / P2a insert** — canon functional QA | ✅ done | mandatory full-set canon tests, canon-vs-copy fault attribution, regression-gated reconcile-back; **phase P2a inserted** to author the suite (governs `SPEC §machinery`) |
| **P2.6 (+amend)** — canon code home | ✅ done | `reference-code/powershell/` 39 Public + 19 Private + `.psm1`/`.psd1`; per-unit markers + norm hash; analyzer config (`.psa.config.json` [PSAP0003/4/5] + `PSScriptAnalyzerSettings.psd1`); marker version = **SemVer** (rNN→0.1.0); full config-aware gate 0/0/0 |
| **ADR 0008** — canon release model | ✅ done | SemVer pre-release guardrail: `0.x` not vendorable; promote to `1.0.0` on full-suite pass; vendoring gate `version >= 1.0.0`; refines ADR 0007 |
| **P2.7** — manifest | ✅ done | `governance/state/manifest.jsonl` 58 rows (`canonical_version 0.1.0`, `tested=false`, `consumers=[]`); `tested` added to manifest schema (`maturity` **deferred to TF** — baseline §2.10 CNCF axis, values open until final stage) |
| **P2.8a** — governance-state gate | ✅ done | `quality-tools/governance-state-validator/` (checks A–F: schema · location · manifest↔marker · canon coverage · canonical-JSON); 0 findings on repo, self-test 6/6 |
| **P2.8b** — STATUS + gate clause | ✅ done | STATUS brought current (`10f513d`); AGENTS.md post-flight §14 = governance-state gate |
| **P2 close** — defer manifest maturity | ✅ done | `867efe3`: `maturity` removed from manifest schema/jsonl (deferred to TF — baseline §2.10 CNCF axis); release-readiness via `tested` + SemVer `version` (ADR 0008). P2 closed here |
| **ADR 0009** — psa.py canonical lifecycle | ✅ done | `964752c` `[AUTH]`: psa.py 4.3.0 (`psa2013_known_script_vars`); the `scripts/python/...` psa.py copy frozen at 4.2.0; canon back to 0/0/0 |
| **P2a-prep / Pgov-charter** — ADR 0010/0011 | ✅ done | `56c3dae` **ADR 0010** (canon test taxonomy + JSONL/DuckDB data management); `7027b16` **ADR 0011** (canon change-management governance: manifest = Git-resident master, tool-mediated CRUD [P3a], one process, impact-weighted decision gate, audit trail) + SPEC Machinery back-refs |
| **P2a.1** — canon test home | ✅ done | `98119ed`: `reference-code/powershell/tests/` harness + `CanonSessionState` fixture + Smoke tests + `common/`; governance-state validator check E corrected disk-master → **manifest-master** (ADR 0011 §1; tests/ never flagged), self-test 7/7 |
| **P2a.2** — behavioral suite (58 units) | ✅ done | `879a0da` U0/U1/U2 (41 Pester) + `f38057a` F-state/F-env (Canon.Env). **ADR 0012** (`a3add27` dual-runtime env-info: CLRVersion → `[System.Environment]::Version`, BuildVersion `.ContainsKey` guard) + **ADR 0013** (`3e96cf3` multi-platform/version QA: 3-cell PSScriptAnalyzer compat matrix + per-unit `platform_scope` + classification-backed suppression + FrameworkDescription removal). Real-host verified on PS 5.1 (`4.0.30319.42000`) + PS 7 Linux (`8.0.10`) |
| **P2a.3a** — canon-test gate runner | ✅ done | `ae73339`: `tests/Invoke-CanonTests.ps1` single entry (Pester Unit/Env/Smoke + T11) → **71 passed / 1 skipped / 0 failed, PASS**; canon-resident (vendored with canon); `psa2010_known_cmdlets` for Pester cmdlets |
| **P2a.3b** — SemVer promotion 1.0.0 | ✅ done | `30b67f1`: all 58 units `canonical_version 0.1.0 → 1.0.0` + `tested true`; markers `version=1.0.0` (hash byte-identical); `.psd1` ModuleVersion 1.0.0. **Canon released, vendorable** (ADR 0008 §4: version ≥ 1.0.0) |
| **P2a.4** — close P2a (STATUS) | ✅ done | STATUS brought current to canon 1.0.0 / P2a complete; next phase P3 |
| **ADR 0014** — document governance model | ✅ done | `8249f2e` `[AUTH]` (doc-only): the DOCUMENT analog of ADR 0007+0008. Three cross-repo doc classes (A reference / B own-and-reconstruct / C vendored-copy); `governance/templates/` = document-template canon at code parity (`kind=template` + SemVer release gate + conformance gate over rendered doc-sets); class-(B) reconstruction = graduation structural transform; every reconstruction/sync = a quality gate. **No new phase / no renumbering** (TF + P4–P7 exits absorb it). governs `SPEC §machinery` (bidirectional back-ref). Instance-level wiring deferred to TF.1 |
| **P3.0 / ADR 0015** — canonical normalized-hash contract (P3 opening) | ✅ done | promoted baseline §4.5 → in-repo SPEC §machinery as a **computable** contract (region body → strip comments/strings → collapse whitespace → `sha256`/**16-hex**; gate=normalized, raw=forensic, forked=frozen, whole-tool=null/`n/a`). Validator **check G** (recompute marker hash) + **check D** extended (marker `policy`/`binding` vs manifest); **`quality-tools/canon-hash-restamp/`** write-side tool (metadata-only); conformance pinned by **golden vectors GV-1..5** (no shared import; ADR 0003 reuse-by-copy). Check G caught **20/58 markers mis-stamped at `5d5f0b1`** → re-stamped **no version bump** (bodies byte-identical). Validator self-test **15/15**. Interim **metadata guardrail**: validator (incl. G) green at §Y before patch; bridges to ADR 0011 CRUD tool (P3a) |
| **P3.1 / ADR 0016** — scanner output-contract pins | ✅ done | documents-only structural review (no scanner code yet) pinned 3 undetermined observation-schema fields: **F1** `runtime.duckdb="n/a"` pre-P8 (ADR 0002; runtime stamped not pinned); **F3** `kind`→`granularity` table in SPEC §machinery (manifest has no `granularity`; powershell-helper/bash-region/spec-region→region, python-helper/python-tool/tool→whole-tool, governance-doc out of body-hash-drift scope per ADR 0014); **F4** `drift=unknown` = determinability fallback (conditions pinned at P6). Description-only schema annotations; no structure/code/canon change. Clarified (no decision): **P3 builds+fixture-tests; first real run P6** |
| **P3.6** — scanner build + self-test + register #9 | ✅ done | `quality-tools/canonical-drift-scanner/scanner.py` + `test_scanner.py` (**12/12**): single-file, stdlib-only, no-cross-reference; reuse-by-copies the ADR 0015 normalizer (GV-1..5), marker parse + region extract, normalized+raw hashes, canonical-JSON emit, `granularity` from `kind` (ADR 0016 F3), `runtime.duckdb="n/a"` (F1), `drift=unknown` fallback (F4). Registered as whole-tool unit **#9** (`tool.canonical-drift-scanner`) via the **sentinel-value convention** (`tested`=self-test green, re-defined for whole-tool; cross-platform/canonical/follow-latest/0.1.0) — AGENTS.md §8, not an ADR (**P4.4 decision owed**). Real run pre-P6: whole-tool rows only (ADR 0016 F2) |
| **P3.7** — phase-end (gates + register front-load + static-point) | ✅ done | **P3 phase closed.** Gate battery green (scanner self-test 12/12 + emitted JSONL passes #3; validator A–G + 15/15; restamp in-sync; psa self-check; PSSA 0/0/0; canon-test 71/1/0). `TEMPLATE-REQUIREMENTS-REGISTER` front-loaded for TF.1 (new **§9 TR-TOOL-1/2/3** machinery-registration, P3.6-discovered; reflected in the Tier-P bundle). Static-point taken. Next: **P3a** |
| **P3a.1** — manifest CRUD tool (build + self-test) | ✅ done | `quality-tools/canon-manifest-tool/tool.py` + `test_tool.py` (**13/13**): single-file, stdlib-only; reuse-by-copies the canonical-JSON `_canonical_line` (ADR 0003); CRUD on `manifest.jsonl` (register region/whole-tool · update tested/consumers/non-coupled · deregister). **Self-validating (ADR 0011 §2):** each op is transactional — apply → run the governance-state validator as a subprocess (no shared import; fail-safe if absent) → **roll back byte-identical on any finding**, so the master is never committed invalid. **Write-path boundary (§Z-signed):** manifest-row write path only; a marker-coupled change (`canonical_version`/`change_policy`/`binding_mode` on a region unit, or deregistering one) is **REFUSED** because the validator's checks **D/E/G** catch the resulting marker drift — boundary enforced by the existing gate, not a special case. The coordinated manifest-row+marker write = the **named-deferred `unit-record coupled write`** (P6/P7). **ADR 0015 §6 guardrail superseded for manifest-row edits** (marker edits stay under it / `canon-hash-restamp` until the coupled write — AGENTS.md §8). Manifest unchanged (self-registration is P3a.3). Dry-run (§Y) green on a throwaway clone: real CRUD round-trip + refuse-path verified + full gate battery re-green |
| **P3a.2** — machine trigger (build + self-test) | ✅ done | `quality-tools/canon-drift-trigger/trigger.py` + `test_trigger.py` (**23/23**): single-file, stdlib-only; `_canonical_line` reuse-by-copy (logic byte-identical). The machine entry point of the change-management process (ADR 0011 §3-machine): reads scanner observation records, and for every `granularity=region` + `drift=drift` row emits a thin structured change-request payload on stdout (sorted, canonical-JSON; one per `(unit_id, consumer, region)`). **Emit-only:** no apply, **no new store** (§5); the decision gate (§4) and the `unit-record coupled write` are deferred to **P7a** / **P6/P7**. Every drift-driven request is `classification=marker-coupled`, `proposed_route` = the deferred path — never an immediately-appliable manifest op (consistent with the P3a.1 boundary; the two are non-contradictory — neither applies a coupled change now). **Provisional contract** (no request schema file; P7a owns it) with `contract_status`/`consumer_status` markers (self-test enforces them = deferral stays detectable). Derived only from drift/identity fields; **DEP-3 fork fields not consumed**; `region_locator` opaque pass-through; `kind`/`impact` null (maturity clause). Manifest unchanged (registration is P3a.3); first **real** run is P6. §Y dry-run green on a throwaway clone (Tier 1 static / Tier 2 fixture 23/23 + live emit / Tier 3 full battery / Tier 4 deferral + boundary) |
| **P3a.3** — register CRUD tool + trigger (whole-tool rows) -> **P3a exit** | ✅ done | Registered `tool.canon-manifest-tool` + `tool.canon-drift-trigger` as homogeneous whole-tool rows (`0.1.0`/`cross-platform`/`canonical`/`follow-latest` sentinels per TR-TOOL-1, `tested=true` per TR-TOOL-2, `consumers=[]`) **via the `canon-manifest-tool` CRUD tool itself** (dogfooding; first production use of the P3a.1 write path — each register transactional: apply -> validator -> roll-back-on-finding, both 0 findings). Manifest **59 -> 61** (58 helper + 3 tool). A tool row is a **manifest-only write**, so the CRUD tool handles it **directly** (not refused) — the in-scope counterpart to the refused marker-coupled write (the P3a.1 boundary's "passes" side). **check E bijection unaffected** (powershell-helper-only; 58<->58). finding C: the two rows are homogeneous and do not move the **[P4.4]** convention re-evaluation earlier. §Y dry-run green on a throwaway clone (register x2 + idempotency-reject + full gate battery: validator 0/A-G + 15/15, psa 0/0/0, scanner 12/12, CRUD 13/13, trigger 23/23, PSSA 0/0/0, canon-test 71/1/0) |
| **TF.1** — declare M5 register complete (cycle-closing gate) | ✅ done | Completeness pass: cross-checked every template/dotfile row against each phase's doc-set #4 (no un-entered requirement). Closed **GAP-1** (kind enum `template` — applied at the TF.2 schema edit), **GAP-2** (`template` out of the scanner's body-hash-drift scope), **GAP-3** (doc-set deltas confirmed on disk). Resolved instance-pins: **maturity** (top-level OPTIONAL; live-schema add + per-unit values deferred to graduation), **version-gate = two-axis (C)** (Axis 1 per-template / Axis 2 `governance/templates/VERSION`; durable in **ADR 0018**), **conformance** (machine C1–C8 + human H1–H3), **reconstruction** (9-step procedure), **marker cosmetic** (frozen frame), **template-canon scope** (GAP-4: class-(B) doc-set only; adr/prompt = scaffolding). M5 register (Tier-P) frozen-complete; no body produced yet. |
| **TF.2** — template bodies + machinery (one batch) | in progress | **ADR 0018** authored (two-axis template-canon version model; refines ADR 0014 §2) + SPEC §machinery back-ref + this STATUS. **Remaining:** `kind` enum `template` schema edit; `governance/templates/VERSION` (Axis 2, 1.0.0); template bodies (README/README.ja/SPEC/TESTING/CHANGELOG + dotfiles) + `kind=template` registration (Axis 1, via the CRUD tool); the document-conformance gate tool (C1–C8, `quality-tools/`, ADR 0003). |

Gates green at HEAD: psa.py 0 (config-aware, incl. the `reference-code/powershell` canon + the canon-test runner via `psa2010_known_cmdlets`) · PSScriptAnalyzer 0/0/0 (standard + the ADR 0013 3-cell compatibility matrix, Public/Private) · governance-state-validator **0 findings (A–G)** (61 manifest rows = 58 `powershell-helper` + 3 `tool` [scanner + CRUD tool + trigger]; 58 helper rows ↔ 58 canon files [check E]; **check G** marker-hash integrity + check D marker `policy`/`binding`; self-test **15/15**) · **canon-test runner PASS (71/1-skip/0)** · **`canon-hash-restamp --check` IN SYNC (58/58)** · **`canon-manifest-tool` self-test 13/13** (P3a.1) · **`canon-drift-trigger` self-test 23/23** (P3a.2). Runtimes: pwsh 7.4.6 · PSScriptAnalyzer 1.25.0 · python3 · jsonschema 4.26.0 · Pester 5.7.1.

## Next action
**Baseline document model (TF.2), re-framed by ADR 0019.** Doc governance is now three layers:
**L1** language-independent format / **L2** language template (common = vendored-from-code +
subproject-specific) / **L3** rendered doc-set. L2-common is **vendored** into L3 via managed
regions (marker + hash + tool-sync), not referenced; provenance = a doc-level YAML front-matter
pin + region-level ADR 0015 markers (two-level staleness trigger); variance = explicit
applicability metadata (no conditional fold-in). **Remaining baseline, dependency order:**
(a) **AGENTS.md §6 evolution** edit (`[AUTH]`, recorded against ADR 0019; TR-SPEC-2 revised);
(b) **L1 format** definition (formalize the M5 register as the language-independent item canon);
(c) **L2-PS common** `governance/spec/powershell.md` (the **deferred P2.6** spec home — vendored
content, region markers + hash, `spec-region` rows); (d) **L2 templates** (PS + bash,
family-parameterized) + `kind=template` registration; (e) **document-conformance gate** (light
structural + region-hash); (f) **reconstruction procedure** freeze; (g) **TF freeze** — template
canon `1.0.0` + static-point. **The 4-bucket program:** (1) governance baseline [P0a–TF] →
(2) same-repo migration [P5 bash, P6 PS] → (3) separate-repo migration [P7 Deploy-Drivers] →
(4) cleanup [psa.py `scripts/` copy; P7a/P8]; **baseline 1.0.0 freeze is the single gate before
migration.** psa.py governance (P4, python-tool family) stays off the critical path. **Carried
deferrals (all tracked, detectable):** **[P4.4]** register `psa.py` (the SemVer/platform-heterogeneous
2nd whole-tool instance) and decide whether to promote the sentinel-value convention (AGENTS.md §8)
to an ADR — `platform_scope` may need a `windows-only` tool to settle sentinel-vs-real; the
**AI-driven trigger + decision gate + machine impact (ADR 0011 §3-AI / §4)** stay at **P7a** (the
trigger's change request is their input; its contract is held provisional/unpinned with
`contract_status`/`consumer_status` markers); the **`unit-record coupled write`** (marker-coupled
manifest+marker write the CRUD tool refuses today) is built at **P6/P7**; the scanner's and trigger's
first **real** run is **P6**, which also pins the three deferred `scanner.py` items (region_locator
final form, `drift=unknown` conditions, the `bash` profile) and the (b) consumer current-state review.
Vendoring (P6/P7) is UNBLOCKED (canon ≥ 1.0.0) but later. Remediate `$ks` at **P6**. The separate
`update-windows-server-iso` work (the §1.1 graduation pilot / a P6 consumer) is also tracked.





## Open pointers
- `[WORKING]` psa.py move **CONTRACT** (psaMove.4/.5 — delete the old `scripts/python/powershell-static-analyzer/` path) is **after P7**, gated on a cross-repo zero-referrer grep + §9.3 checkpoint. Both paths coexist until then.
- `[WORKING]` `Deploy-Drivers` 8 references to the old psa path are migrated in **P7** (single PR).
- `[WORKING]` Subproject migration `scripts/<family>/<name>/ → projects/<lang>-<name>/` is **P5/P6**; `reference-code/powershell/` is **released at 1.0.0 (P2a.3b)** — 58 units, full config-aware gate + compatibility matrix + governance-state gate + canon-test runner all green; **vendorable** (ADR 0008 §4); bash/python canon homes still **deferred** (SPINE-2/5; policies decided).
- `[WORKING]` Governance docs for psa.py at its new home = **P4**; template finalization (TF) = before **P5**. **[ADR 0014]** TF now also freezes the **document-conformance gate** + the **class-(B) reconstruction procedure** + `kind=template` registration/version gate (the document side at code parity); `governance/templates/` is the document-template canon. **[ADR 0014 §2 scope clarification, 2026-06-04]** the canon governs **documents + masterless repo-structural dotfiles**, **NOT tool-owned configs** (e.g. `.psa.config.json`, master = the tool canon, follow-latest per ADR 0009) — discriminator = **master-ownership**; stated in full in the Layer-1 document-format definition (TF (b)).
- `[WORKING]` **TF (b) — Layer-1 document-format canon authored.** `governance/doc-format/` now holds the L1 item canon: **`doc-format.jsonl`** (58 MECE format items — README/SPEC/TESTING/CHANGELOG/dotfile/community-health — each with `member`/`order`/`role`/`content_model`/`bilingual`/`applicability{families,topologies,presence}`/`source`; language-independent, light/no-hash), its **`doc-format.schema.json`** (draft-07; carries the 3-layer model + the master-ownership scope policy = the "Layer-1 document-format definition" of ADR 0014 §2 / ADR 0019), and **`VERSION` = 0.1.0** (the `layer-1-format` front-matter pin, ADR 0019; -> 1.0.0 at the TF freeze). Heavy marker+hash machinery is reserved for `content_model=vendored` items (SPEC Part A, sourced from the L2-common spec home). Validated offline against its own schema (58/0). Next TF sub-steps: (c) L2-PS spec home `governance/spec/powershell.md`, (d) L2 templates + `kind=template` rows, (e) document-conformance gate, (a) AGENTS.md §6 reconciliation, (f) reconstruction freeze, (g) TF freeze.
- `[WORKING]` **TF (c) — Layer-2 PowerShell spec home authored.** `governance/spec/powershell.md` (deferred-P2.6 spec home) now holds the **14 vendored Part A regions** (A.1–A.13 + the A.9 JSONL split), each delimited by an **ADR 0015 Markdown marker** (`<!-- >>> CANONICAL unit_id=spec.powershell.part-a.<x> version=0.1.0 hash=PENDING policy=canonical binding=follow-latest >>> -->`). Normalised to **common convention only** (D1: A.11 = gate + 3-stage CI model, project disables/CI filenames → consumer; D2: A.8's A.14 dependence conditionalised; D3: A.12 keeps doc-language policy/file-set, README disclaimer/license owned by the `readme.*` items; D4: A.9 split csv-required + jsonl-optional; D5: A.10 phase-number generalised). **Registration model (B, user-confirmed):** ONE file-level `kind=spec-region` manifest row (`unit_id=spec.powershell.part-a`, `canonical_location=governance/spec/powershell.md`, 0.1.0, `tested=false`); the per-region identity is owned by **L1** (`doc-format.jsonl` spec.part-a.* items) + the markers, and **region-level conformance is the document-conformance gate's job (TF (e))**. **HASH = PENDING** by design: the doc-region normalized-hash contract + stamp/verify path are the doc gate's (TF (e), a SEPARATE tool from the code/state validator, per the confirmed separation); the governance-state validator's D/G stay `kind=powershell-helper`-scoped and correctly skip spec-region (gate C = file existence only). Manifest now 62 rows.
- `[WORKING]` **TF (d1) — scaffolding templates re-prefixed `scaffold-`.** `governance/templates/{adr,change-authoring-prompt,session-start-prompt}.template.md` -> `scaffold-*` (authoring scaffolding, GAP-4; NOT rendered doc-set canon, NOT manifest-registered). Establishes the flat-with-prefix taxonomy before the rendered doc-set templates land (`powershell-`/`bash-`/`python-` language sets, `repo-` repo-level). Pure rename: no in-repo references by filename existed (docs reference the `governance/templates/` directory generically), no manifest change. TF (d) proceeds in sequenced patches: (d1) scaffold rename [this], (d2) `repo-` templates + `kind=template` rows, (d3) `powershell-` doc-set templates, (d4) `bash-`/`python-` templates.
- `[WORKING]` **TF (d2a) [AUTH] — community-health bilingual correction (canon ↔ current-state alignment, ADR 0017).** Current-state pass found the live `CODE_OF_CONDUCT` / `CONTRIBUTING` / `SECURITY` are **in-file bilingual** (English + Japanese sections in one file) and that `CONTRIBUTING`/`SECURITY` are substantially repo-specific - both at odds with the (b) L1 rows. Corrected L1 (`doc-format.jsonl`): the three community-health items -> **`bilingual=true`**; `CONTRIBUTING`/`SECURITY` -> **`content_model=mixed`** (common skeleton + boilerplate + repo-specific fill, per D-CH2). `LICENSE` unchanged (MIT, English-only, common-parameterized). Broadened the spec home **A.12** region: Japanese is permitted in the README twin-file pair AND the in-file-bilingual community-health files; the bilingual **mode** is member-derived (README = twin-file, community-health = in-file), so no L1 schema field is added. (d2) `repo-` whole-file templates author on this corrected canon.
- `[WORKING]` **TF (d2 / tool) — canon-manifest-tool KINDS synced with the schema enum.** Registering `kind=template` rows failed: the CRUD tool's hardcoded `KINDS` tuple was missing `template` (the schema's `kind` enum gained it at TF.2 / GAP-1, but the tool was not updated). Added `template` to `KINDS` (the tuple is mirrored from `governance/schema/manifest.schema.json`) + a `register kind=template` regression test (`test_tool.py` 13 -> 14). Prerequisite enabler for all `kind=template` registration in TF (d).
- `[WORKING]` **TF (d2) — `repo-` dotfile templates.** Added the family-agnostic (families=all) whole-file dotfile templates under `governance/templates/`: **`repo-gitattributes.template`** (common-fixed encoding/binary/linguist contract), **`repo-gitignore.template`** (common-parameterized: universal patterns + a consumer fill section), **`repo-pull-request-template.template.md`** (common-parameterized). Registered 3 `kind=template` rows (`template.dotfile.{gitattributes,gitignore,github-pr-template}`, 0.1.0, `tested=false`); manifest 62 -> 65. The PR template carries the **corrected** documentation-language policy (per D-CH1: README twin-file + COC/CONTRIBUTING/SECURITY in-file bilingual; SPEC/TESTING/CHANGELOG/LICENSE English-only). **Reconciliation owed (current artifacts):** the live `.github/pull_request_template.md` and the root `README.md` "Language Policy" section still state the pre-D-CH1 English-only claim for COC/CONTRIBUTING/SECURITY; they are reconstructed from the corrected templates at migration (P6/P7), not hand-edited now.
- `[WORKING]` **TF (d2c) — `repo-` community-health templates.** Added the four community-health templates under `governance/templates/`: **`repo-license.template`** (common-parameterized: MIT + `{{COPYRIGHT_YEAR}}`/`{{COPYRIGHT_HOLDER}}`, English-only), **`repo-code-of-conduct.template.md`** (common-fixed, in-file bilingual - the canonical COC verbatim), **`repo-contributing.template.md`** and **`repo-security.template.md`** (both `content_model=mixed`, in-file bilingual: common skeleton + common boilerplate - reporting/response, PR checklist, commit convention, license - with `{{TOKEN}}` substitutions and `<!-- FILL: ... -->` regions for repo-specific prose). Registered 4 `kind=template` rows (`template.community-health.{license,code-of-conduct,contributing,security}`, 0.1.0, `tested=false`); manifest 65 -> 69, template rows 3 -> 7. The three bilingual templates intentionally carry Japanese as sanctioned canonical content (in-file bilingual per D-CH1); `repo-license.template` and all non-template governance files stay ASCII.
- `[WORKING]` **TF (d3a) — `repo-` README CORE template (twin-file bilingual).** Per decision A1 (a twin-file bilingual doc is modeled as two template files mirroring its `.md` + `.ja.md` output), added `governance/templates/repo-readme-core.template.md` (English half -> README.md, strictly ASCII) and `repo-readme-core.ja.template.md` (Japanese half -> README.ja.md, sanctioned Japanese). They realize the nine L1 `readme.*` items whose `applicability.families = all` (disclaimer, license, why-exists, folder-layout, quick-start, ci-status-table, configuration, self-verification, troubleshooting) in L1 order, with `<!-- ASSEMBLE -->` markers at the gaps where per-language/feature SUPPLEMENT items (L1 order 6-10, 14-15) interleave. content_model handling: common-fixed (disclaimer/license) authored verbatim; common-parameterized (why-exists/folder-layout/quick-start/ci-status) as common skeleton + `{{TOKEN}}`/`<!-- FILL -->`; specific (configuration/self-verification/troubleshooting) as canonical heading + role note + FILL. **No doc-region hash markers were added:** the doc-region marker/hash contract is owned by TF (e) and must cover parameterized/specific content (where a section-level canonical hash does not fit), so markers are applied uniformly to the spec home + all doc-set templates there, not piecemeal here. Registered 2 `kind=template` rows (Model B, one per file): `template.readme-core` + `template.readme-core.ja` (0.1.0, tested=false); manifest 69 -> 71, template rows 7 -> 9. Next: d3b (`powershell-` README SUPPLEMENT, twin-file bilingual).
- `[WORKING]` **TF (d3b) — `powershell-` README SUPPLEMENT template (twin-file bilingual).** Added `governance/templates/powershell-readme-supplement.template.md` (English -> README.md, ASCII) and `.ja.template.md` (Japanese -> README.ja.md). Realizes the five L1 `readme.*` items whose `applicability.families` includes powershell, as content_model=specific stubs (canonical heading + role/FILL guide; project content filled at L3), in L1 order: action-reference (6), phase-reference (7), parameters (8) [shared families=[powershell,bash]], risk-classification (14), hardware-os-scope (15) [powershell-only]. Grouped to match the repo- README CORE ASSEMBLE points: GROUP 1 (order 6-8) interleaves after CORE "CI status", GROUP 2 (order 14-15) after CORE "Troubleshooting". **Shared-subset homing:** action/phase/parameters are families=[powershell,bash] (a subset, not covered by the single-language vs families=all prefix axis); resolved by realizing them per-applicable-language - the bash- supplement (d4) realizes the same three L1 items. Since they are content_model=specific (heading+role+FILL only), there is no project-content duplication; L1 owns identity and TF (e) verifies stub<->L1 coherence. No new prefix introduced (consistent with the rejected `common-` prefix). Registered 2 `kind=template` rows: `template.powershell.readme-supplement` + `.ja` (0.1.0, platform_scope=cross-platform, tested=false); manifest 71 -> 73, template rows 9 -> 11. Next: d3c (`powershell-` SPEC skeleton).
- `[WORKING]` **TF (d3c) — `powershell-` SPEC skeleton template (English-only).** Added `governance/templates/powershell-spec.template.md`. SPEC is English-only per the canonical doc-language-policy (spec home A.12) - there is **no SPEC.ja twin**, so this is a single ASCII file (confirmed against the repo: no `SPEC.ja.md` exists anywhere). **Part A by reference (decision B1):** the 14 canonical Part A regions are NOT restated; an `ASSEMBLE` directive lists their unit_ids (in canonical order A.1-A.13) and records that they are vendored (L2 -> L3) from the spec home `governance/spec/powershell.md` at reconstruction, honoring the AGENTS.md §6 Part A Inheritance Rule (no cross-L2 copy). **Parts B/C/D** authored as the script-specific skeleton: Part B with canonical structural sections B.1 Identification / B.2 Inputs / B.3 Outputs / B.4 Phase Map + FILL for B.5+; Part C with the common gate categories (Static / CI / Functional / Documentation) stating the common gates + FILL, plus conditional Cross-data and Debug-Trace categories; Part D as an append-only pitfalls list + the seed appendix. No doc-region hash markers (TF (e) owns the contract). Registered 1 `kind=template` row `template.powershell.spec` (0.1.0, cross-platform, tested=false); manifest 73 -> 74, template rows 11 -> 12. Next: d3d (`powershell-` TESTING template).
- `[WORKING]` **TF (d3d) — `powershell-` TESTING skeleton template (English-only).** Added `governance/templates/powershell-testing.template.md` (English-only per doc-language-policy A.12; no .ja twin). Realizes the six L1 `testing.*` items in L1 order: status-summary (0), static-analysis-gate (1), discovered-bugs (2), ci-coverage (3), verification-procedure (4) [all families=[powershell,bash]], validation-results (5) [powershell-only]. content_model handling: common-parameterized (status-summary/static-analysis-gate/ci-coverage) as common skeleton + FILL (status table; psa.py 0/0/0 gate procedure; multi-stage CI model); specific (discovered-bugs/verification-procedure/validation-results) as heading + role + FILL. Items 0-4 are shared with bash (the bash- TESTING template at d4 realizes the same five); validation-results is powershell-only. No doc-region hash markers (TF (e)). Registered 1 `kind=template` row `template.powershell.testing` (0.1.0, cross-platform, tested=false); manifest 74 -> 75, template rows 12 -> 13. Next: d3e (changelog format: `repo-` changelog + `powershell-`).
- `[WORKING]` **TF (d3e) — `repo-` CHANGELOG template (English-only) — d3 COMPLETE.** Added `governance/templates/repo-changelog.template.md`. **Scope correction confirmed against L1:** both `changelog.*` items are `applicability.families = all` (changelog.format ord 0 / common-fixed; changelog.entries ord 1 / specific), so CHANGELOG format is language-independent - a single `repo-` template, not the `repo- + powershell-` pair earlier hypothesized; bilingual=false so English-only, no .ja twin. Realizes changelog.format as the common-fixed Keep-a-Changelog 1.1.0 header (with {{PROJECT_TITLE}} + a FILL for the project versioning scheme + the English-only policy note) and changelog.entries as the specific [Unreleased] + released-version skeleton with the Keep-a-Changelog categories + FILL. No doc-region hash markers (TF (e)). Registered 1 `kind=template` row `template.changelog` (0.1.0, cross-platform, tested=false); manifest 75 -> 76, template rows 13 -> 14. **TF (d3) doc-set templates complete:** README CORE (d3a) + powershell README SUPPLEMENT (d3b) + powershell SPEC skeleton (d3c) + powershell TESTING (d3d) + repo CHANGELOG (d3e). Remaining doc-set work is the bash-/python- supplements (TF d4) and the document-conformance gate (TF e).
- `[WORKING]` **TF (d4) — `bash-` / `python-` README SUPPLEMENT templates (twin-file bilingual).** Added `governance/templates/bash-readme-supplement.template.md` (+`.ja`) realizing the three shared L1 readme items for bash - action-reference (6), phase-reference (7), parameters (8) [families=[powershell,bash]]; and `python-readme-supplement.template.md` (+`.ja`) realizing rule-catalog (9), output-format (10) [families=[python]]. All content_model=specific stubs (heading + role/FILL); both interleave into the repo- README CORE at ASSEMBLE GROUP 1 (after "CI status"); neither family has L1 order 14-15 items so there is no GROUP 2. The bash action/phase/parameters items are the same L1 items the powershell- supplement realizes (L1 owns identity; no project-content duplication). Registered 4 `kind=template` rows: `template.bash.readme-supplement`(+.ja), `template.python.readme-supplement`(+.ja); manifest 76 -> 80, template rows 14 -> 18. SPEC/TESTING skeletons for bash/python are deferred - they require bash/python spec homes (not yet built), unlike the powershell spec home that already exists. Next family work: bash/python spec homes, then their SPEC/TESTING skeletons.
- `[ACCEPTED]` **TF (e) step 1 — ADR 0020 (doc-region normalized-hash contract + document-conformance gate boundary).** Authored `governance/adr/0020-doc-region-normalized-hash-contract.md` (status: proposed - the design gateway for the TF (e) gate). Fixes: (1) a doc-region normalized hash defined for **common-fixed regions only** (strip HTML comments -> LF -> trim trailing ws -> collapse blank-line runs -> sha256 -> first 16 hex); (2) **common-parameterized / specific regions are NOT hash-pinned** - structural conformance only (present, correct unit_id/L1-order/heading-role, required TOKEN/FILL markers); (3) the **document-conformance gate owns all doc-region inspection** and stamps PENDING -> real (common-fixed) / structural sentinel (rest), across the spec home + doc-set templates; (4) the governance-state validator (gates D/G) and canon-hash-restamp stay reference-code-scoped and are NOT extended to markdown. This resolves the TF d3/d4 "no markers yet" deferral (the gate adds them). **Follow-on (after this ADR is accepted):** build the gate (stamp/verify + structural + front-matter pin + L1-membership), add the contract text to governance/SPEC.md §machinery, roll markers across the spec home + all doc-set templates. **Acceptance refinement (content review):** the hashed set is the verbatim-canonical content models `common-fixed` AND `vendored` (the spec home Part A regions are `content_model=vendored` in L1 - the primary hashed target per ADR 0019), and `mixed` joins the structural set; so hash={common-fixed,vendored}, structural={common-parameterized,specific,mixed}.
- `[WORKING]` **TF (e) step 2 — document-conformance gate tool.** Added `quality-tools/document-conformance-gate/doc_gate.py` + `test_doc_gate.py` (22/22), registered as `kind=tool` unit `tool.document-conformance-gate` (manifest 80 -> 81, tools 3 -> 4). Implements the ADR 0020 contract: `doc_region_norm_hash` (strip HTML comments -> collapse all whitespace per ADR 0015 convention -> sha256 16hex) for verbatim-canonical regions (common-fixed / vendored); structural conformance (policy=structural, hash=NONE sentinel, non-empty body) for common-parameterized / specific / mixed; marker coherence (pairing / unit_id agreement / no duplicates / no nesting); L1 item-membership with family-segment stripping (`spec.powershell.part-a.X` -> `spec.part-a.X`); doc-level YAML front-matter provenance pin (ADR 0019). Modes: `--check` (default; exit 1 on findings) and `--stamp` (PENDING -> real hash / structural sentinel). Pre-roll-out, `--check` over the repo correctly reports the 14 spec-home Part A `vendored` regions as `hash=PENDING` (needs stamping) - this is the work e3 performs. Tool boundary honored: the governance-state validator (D/G) and canon-hash-restamp remain reference-code-scoped (unchanged). Next: e3 (marker roll-out: stamp the spec home + add markers to the doc-set templates), then e4 (governance/SPEC.md §machinery contract text).
- `[WORKING]` **TF (e) step 3a — stamp the spec home.** Ran `doc_gate.py --stamp` over `governance/spec/powershell.md`: the 14 Part A `vendored` regions are now stamped from `hash=PENDING` to real doc-region hashes (e.g. reference-assets=d1f03d5c548d4f3f). `doc_gate --check` over the whole repo is now **0 findings** (spec home pinned; templates carry no markers yet so they pass vacuously). The code-side `canon-hash-restamp --check` remains IN SYNC and the governance-state validator stays green - the doc-region hash lives only in the spec-region markdown markers and does not touch reference-code (ADR 0020 tool boundary). **Deferred to its own unit - e3b (template marker roll-out):** wrapping each doc-set template section in canonical/structural markers (common-fixed/vendored -> canonical + real hash; parameterized/specific/mixed -> structural sentinel) is a large per-file pass (18 templates, section -> L1-item mapping) and will be done as a focused, gate-verified unit.
- `[WORKING]` **Governance ASCII hygiene** - English-ized the lone CJK term gloss in `governance/static-point-procedure.md` (`"static point" (...)` Japanese gloss -> romaji `(seishi-ten)`), so every non-bilingual governance file is strictly English/ASCII. (Found via a repo-wide Python CJK audit after the interactive `grep -P` Unicode-class check proved unreliable in this environment; the audit confirmed no session-authored governance file carried stray CJK.)
- Known gate deviation: `Update-WindowsServerIso.ps1` PSScriptAnalyzer `0/1/0` (`$ks`) — user-dispositioned, remediate at **P6**. **[ADR 0014 §4]** locate it by **symbol/context, not a fixed line number** (the script evolves; the line drifts), as part of the P6 conformance pass (§8.3; not accepted-open).
- **Metadata guardrail (ADR 0015 §6) — superseded for manifest-row edits at P3a.1.** `manifest.jsonl` rows now go only through the self-validating `quality-tools/canon-manifest-tool/` (transactional apply → validator → roll-back-on-finding); direct manifest edits stop. `[WORKING]` the guardrail still governs **canonical-marker edits** (region `version`/`policy`/`binding`/`hash`), which the tool does NOT write: verification-before-patch (validator incl. **check G** green at the §Y dry-run, before `git format-patch`) + marker `hash=` via **`canon-hash-restamp`**, until the coupled manifest-row+marker write (the deferred **`unit-record coupled write`**) is built at **P6/P7**.
- **ADR 0006:** P1 ships **no contract-CI** (governance-loading wiring is advisory, not CI-enforced). Revisit at **P5/P6** when file moves raise the regression risk — add the contract check then via a new ADR if warranted.
- `[WORKING]` **PSAP0005 default-on** in `psa.py` is a **roadmap** item (psa.py rule-default change + tests + version bump). Canon markers already use a SemVer `version=` (non-rNN), so they stay PSAP0005-clean when it flips on.
- The PowerShell gate (AGENTS.md **§9**) is now the **full config-aware gate** (psa.py config-aware + PSScriptAnalyzer + pwsh ParseFile + import/tests; stand up runtime per baseline §8.2; "no pwsh/deferred" = deviation, M4(A)); the **governance-state gate** (AGENTS.md **§14**) covers `governance/state` + `governance/schema` via the validator (P2.8a).
- **Commit-author identity (standing; AGENTS.md §5 "Commit-author identity", §8 pre-flight #5).** Claude authors patch commits as **`Claude <claude@anthropic.com>`** (optional phase parenthetical, e.g. `Claude (TF.2 author)`); the contributor's `git am`/push supplies the committer — the form of commit `eb34339`. Ad-hoc/per-session names (`AI Agent`, `session@local`, …) are not used; phase context goes in the commit subject.

## ADR index (`../adr/`)
- 0001 — tooling language = Python · 0002 — analysis layer = DuckDB (disposable, P8) · 0003 — standalone-tool principle · 0004 — outcome-based execution framework (M8) · 0005 — session-handoff protocol · 0006 — AI-agent config coverage & contract-CI scope · **0007 — canon code functional QA** (P2a) · **0008 — canon release model** (SemVer pre-release guardrail; refines 0007) · **0009 — psa.py canonical lifecycle** (single canonical psa.py at `quality-tools/`; `scripts/` copy frozen) · **0010 — canon test taxonomy + data management** (test buckets; JSONL master + DuckDB ephemeral) · **0011 — canon change-management governance** (manifest = Git-resident master, tool-mediated CRUD, one process, impact-weighted decision gate, audit trail) · **0012 — dual-runtime env-info policy** (equivalent-quality info on PS 5.1 + 7.x) · **0013 — multi-platform / multi-version QA** (3-cell compat matrix + per-unit `platform_scope` + classification-backed suppression) · **0014 — document governance model** (cross-repo doc classes A/B/C; `governance/templates/` = document-template canon at code parity with version + conformance gate; class-(B) reconstruction incl. the graduation structural transform; every reconstruction/sync = a quality gate; the DOCUMENT analog of ADR 0007+0008; no new phase, TF + P4–P7 exits absorb it; **scope clarification 2026-06-04: governs documents + masterless repo-structural dotfiles, NOT tool-owned configs (e.g. `.psa.config.json`, master = tool canon, follow-latest per ADR 0009) — discriminator = master-ownership**) · **0015 — canonical normalized-hash contract** (the computable hash definition promoted from baseline §4.5: region body → strip comments/strings → collapse whitespace → `sha256`/16-hex; validator read-side **check G** + marker `policy`/`binding` check; write-side `canon-hash-restamp` tool; golden-vector conformance; interim metadata guardrail bridging to the ADR 0011 CRUD tool; corrected 20 markers mis-stamped at `5d5f0b1`, no version bump). · **0016 — scanner output-contract pins** (P3.1; documents-only structural review: `runtime.duckdb="n/a"` pre-P8 [F1], the `kind`→`granularity` derivation table in SPEC §machinery [F3], and `drift=unknown` as the determinability fallback framed now / detailed at P6 [F4]; description-only schema annotations; no structure/code change). · **0017 — structure-first review methodology** (a→b: verify structural validity in the normative set first, investigate current state/current artifacts second; re-scan + forward/back impact analysis + now-vs-later split on each finding; the epistemic-order complement to ADR 0004; finalized after P3's full cycle). · **0018 — template-canon version model (two-axis)** (refines ADR 0014 §2: the template canon carries TWO version axes — **Axis 1** a per-template body `canonical_version` for provenance; **Axis 2** a single `governance/templates/VERSION` that is the reconstructability gate + the consumer pin target. The gate axis follows the unit of delivery — per-unit for vendored code (ADR 0008), per-set for reconstructed doc-sets (ADR 0014 class-(B)). Axis 2 is a release-commit artifact outside the manifest, like the code canon's `.psd1` ModuleVersion, so the ADR 0011 CRUD manifest-row write-path boundary is unaffected). · **0019 — document vendor model + provenance embedding** (refines ADR 0014; evolves AGENTS.md §6 + TR-SPEC-2: three-layer doc model — **L1** language-independent format / **L2** language template [common = vendored-from-code + subproject-specific] / **L3** rendered doc-set; L2-common is **vendored** into L3 via a managed region (marker+hash+tool-sync), not referenced; provenance embedded two ways — doc-level YAML front-matter pin + region-level ADR 0015 markers — feeding a two-level staleness trigger; variance = explicit applicability metadata, no conditional fold-in; heavy marker/hash machinery localized to L2-common while L1/L3 stay light structural conformance).
- Cross-cutting current-truth view: **`../SPEC.md`** — §tooling/§analysis-layer/§machinery/§execution-framework ← ADR 0001–0004; **§machinery also ← ADR 0007/0008/0011/0012/0013/0014/0018/0019** (bidirectional `governs`↔back-ref).

## Design substrate (Tier-P handoff — referenced, not restated, R3)
- `HANDOFF-baseline-consolidated-design.md` — the living design baseline.
- `IMPLEMENTATION-PLAN-skeleton.md` — the living implementation plan (§0.1 disciplines, §Y dry-run incl. Y.1 P0a + Y.5 psaMove, §Z sign-off).
- `TRACEABILITY-SPINE.md` — outcome→deliverable→producer→dependency→gate spine (orphans = 0).
- `TEMPLATE-REQUIREMENTS-REGISTER.md` — Tier-P template/dotfile requirements register.

_Per **ADR 0005**: the four Tier-P docs stay **unmanaged** (out of repo) and are delivered as **one bundle (zip) + MANIFEST** at each static-point — never piecemeal, never assume a current local copy._

## Static-point index (recoverable points = commits; repo-external Zips per `../static-point-procedure.md`)
| Phase | Commit | Static-point |
|:--|:--|:--|
| P0a | `5490de3` (+ `de39ef9` cleanup) | per procedure (repo-external Zip) |
| psaMove.1 | `f289933` | per procedure |
| psaMove.2 | `f8b942b` | per procedure |
| P1 (governance model) | `1097309`·`dd8f9f3`·`cab8375`·`9a05635`·`9be1056` | per procedure (repo-external Zip at P1 phase-end) |
| P2 (analyze + canon + manifest + gate) | `597638e` … `867efe3` | _repo-external Zip at P2 phase-end_ |
| P2a (canon test suite + release 1.0.0) | `964752c` … `30b67f1` | _repo-external Zip at P2a phase-end (this phase)_ |
| P3 (normalized-hash contract + drift scanner) | `30b67f1` … `90117ca` | per procedure (ADR 0015/0016 + scanner; P3 phase-end) |
| P3a (canon change-mgmt machinery: CRUD tool + trigger + registration) | `90117ca` … `648033e` (registration) · `59c100c` (P3a STATUS exit) | _repo-external Zip at P3a phase-end (this phase)_ |
