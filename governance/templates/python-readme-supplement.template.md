<!--
  python- README SUPPLEMENT template (English half; renders into the project README.md).
  Realizes the L1 doc-format readme items whose applicability.families = [python], as
  content_model = specific stubs (canonical heading + role / FILL guide). In L1 order:
     9   readme.rule-catalog   [python]  conditional
     10  readme.output-format  [python]  conditional
  These interleave into the repo- README CORE at its ASSEMBLE GROUP 1 point (after the
  CORE "CI status" section), by L1 order. python has no items in L1 order 14-15 (no
  GROUP 2). L1 owns section identity; TF (e) verifies stub <-> L1 coherence.
  Twin-file bilingual: this English half is strictly ASCII; the Japanese half is
  python-readme-supplement.ja.template.md, kept in lock-step.
-->

<!-- ===== ASSEMBLE GROUP 1: interleaves into the repo- README CORE after "CI status" (L1 order 9-10) ===== -->

## Rule catalog

<!-- readme.rule-catalog (L1 order 9; [python]; conditional - include only if the tool
     is rule-based, e.g. a linter / analyzer).
     FILL: the catalog of rules - rule ID, category, what it flags, default severity,
     and whether it is enabled by default. Note any rules disabled by default and why. -->

## Output format

<!-- readme.output-format (L1 order 10; [python]; conditional).
     FILL: the output formats the tool emits (e.g. human-readable text, JSON, SARIF),
     how to select each, and a short example of the default format. -->
